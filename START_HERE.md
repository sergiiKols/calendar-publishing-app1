# 🚀 START HERE - Развертывание Calendar App

> **Это главный документ для начала работы с Docker развертыванием**

---

## 📖 Что это за проект?

**Calendar Publishing App** - автоматическая система планирования и публикации контента в социальные сети через визуальный календарь.

### Технологии:
- **Frontend/Backend**: Next.js 14 + React 18 + TypeScript
- **База данных**: PostgreSQL 15
- **Deployment**: Docker + Docker Compose

### Возможности:
✅ Прием статей из SMI проекта через API  
✅ Визуальный календарь с drag & drop  
✅ Автопубликация по расписанию (каждые 15 минут)  
✅ Поддержка: WordPress, Telegram, Facebook, Instagram, LinkedIn  
✅ Шифрование токенов  

---

## 🎯 Что было создано для Docker?

### Готовая конфигурация включает:

**🐳 Docker файлы (4)**
- `Dockerfile` - оптимизированный multi-stage build
- `docker-compose.yml` - 3 сервиса (App + PostgreSQL + Cron)
- `.dockerignore` - исключения для сборки
- `.env.docker.example` - шаблон переменных окружения

**📚 Документация (7)**
- `START_HERE.md` ← **ВЫ ЗДЕСЬ**
- `DEPLOY_CHEATSHEET.md` - быстрая шпаргалка
- `DEPLOYMENT_CHECKLIST.md` - пошаговый чеклист
- `QUICK_START_DOCKER.md` - быстрый старт (5 минут)
- `TRANSFER_TO_SERVER.md` - способы переноса на сервер
- `DOCKER_DEPLOYMENT.md` - полное руководство
- `ARCHITECTURE_DOCKER.md` - архитектура и схемы

**🛠️ Скрипты (2)**
- `deploy.sh` - автоматическое развертывание
- `docker-helpers.sh` - вспомогательные команды

**🌐 Дополнительно (1)**
- `nginx.conf.example` - конфигурация для HTTPS

---

## 🏁 Быстрый старт (3 шага)

### Вариант для опытных пользователей:

```bash
# 1. Настроить переменные окружения
cp .env.docker.example .env
nano .env  # Изменить пароли!

# 2. Запустить
docker-compose up -d

# 3. Открыть в браузере
http://your-server:3000
```

**Время развертывания: ~5 минут**

---

## 📋 Полный процесс для новичков

### Шаг 1: Подготовьте сервер

**Требования:**
- Linux сервер (Ubuntu/Debian рекомендуется)
- 2GB RAM минимум
- 2GB свободного места
- Docker и Docker Compose установлены

**Установка Docker (если нужно):**
```bash
sudo apt update
sudo apt install -y docker.io docker-compose
sudo usermod -aG docker $USER
# Выйти и войти снова
```

**Проверка:**
```bash
docker --version
docker-compose --version
```

---

### Шаг 2: Перенесите проект на сервер

**Выберите один из способов:**

#### A) Через Git (рекомендуется)
```bash
git clone <your-repo-url>
cd calendar-app
```

#### B) Через SCP
```bash
# На вашем компьютере
scp -r calendar-app user@server:/home/user/

# Затем на сервере
ssh user@server
cd /home/user/calendar-app
```

#### C) Через архив
```bash
# На компьютере
tar -czf calendar-app.tar.gz calendar-app/
scp calendar-app.tar.gz user@server:

# На сервере
tar -xzf calendar-app.tar.gz
cd calendar-app
```

📖 **Подробнее:** `TRANSFER_TO_SERVER.md`

---

### Шаг 3: Настройте переменные окружения

```bash
# Скопировать шаблон
cp .env.docker.example .env

# Открыть для редактирования
nano .env
```

**⚠️ ОБЯЗАТЕЛЬНО изменить:**

```env
POSTGRES_PASSWORD=ваш_сложный_пароль_123
NEXTAUTH_SECRET=сгенерируйте_openssl_rand_base64_32
CALENDAR_API_KEY=ваш_уникальный_api_ключ
CRON_SECRET=ваш_секрет_для_cron
```

**Генерация секретов:**
```bash
# Для NEXTAUTH_SECRET
openssl rand -base64 32

# Для остальных ключей
openssl rand -hex 16
```

**Если есть домен:**
```env
NEXTAUTH_URL=https://your-domain.com
```

---

### Шаг 4: Запустите Docker

```bash
# Запустить все сервисы
docker-compose up -d

# Проверить статус
docker-compose ps

# Должно быть:
# calendar-postgres  Up
# calendar-nextjs    Up
# calendar-cron      Up
```

**Проверка логов:**
```bash
docker-compose logs -f
# Ctrl+C для выхода
```

---

### Шаг 5: Создайте администратора

**Откройте в браузере:**
```
http://your-server-ip:3000/api/reset-admin
```

**Или через curl на сервере:**
```bash
curl http://localhost:3000/api/reset-admin
```

**Вы получите:**
```json
{
  "success": true,
  "credentials": {
    "username": "admin",
    "password": "admin123"
  }
}
```

**⚠️ Запишите эти данные!**

---

### Шаг 6: Войдите в систему

**Откройте:**
```
http://your-server-ip:3000/login
```

**Войдите:**
- Username: `admin`
- Password: `admin123`

**✅ Сразу смените пароль в настройках!**

---

### Шаг 7: Проверьте работу

- [ ] Календарь открывается: `/calendar`
- [ ] Можно создавать события
- [ ] База данных работает
- [ ] Cron запущен (проверить через 15 минут)

```bash
# Проверка БД
docker-compose exec postgres psql -U calendar_user calendar_db -c "\dt"

# Мониторинг
docker stats
```

---

## 🎉 Готово!

Ваш Calendar App развернут и работает!

---

## 📚 Что дальше?

### Базовая настройка:

1. **Смените пароль админа** (в интерфейсе)
2. **Настройте firewall**
   ```bash
   sudo ufw allow 22/tcp
   sudo ufw allow 3000/tcp
   sudo ufw enable
   ```
3. **Настройте backup БД**
   ```bash
   # Создать папку
   mkdir ~/backups
   
   # Добавить в crontab
   crontab -e
   # Добавить строку:
   0 3 * * * cd /path/to/calendar-app && docker-compose exec postgres pg_dump -U calendar_user calendar_db > ~/backups/calendar_$(date +\%Y\%m\%d).sql
   ```

### Git Workflow и Автоматизация:

4. **Настройте Git репозиторий** (см. `GIT_WORKFLOW.md`)
   - Создайте репозиторий на GitHub/GitLab
   - Настройте автоматический деплой через GitHub Actions
   - Используйте скрипты `deploy-to-server.sh` для деплоя

5. **Разработка через Git:**
   ```bash
   # На компьютере - разработка
   git add .
   git commit -m "Add new feature"
   git push origin main
   
   # Деплой на сервер (3 варианта):
   # 1. Автоматически через GitHub Actions
   # 2. ./deploy-to-server.sh (с компьютера)
   # 3. ssh server && ./server-update.sh (на сервере)
   ```

### Продвинутая настройка:

6. **Настройте домен и HTTPS** (см. `nginx.conf.example`)
7. **Подключите платформы для публикации** (в интерфейсе)
8. **Интегрируйте с SMI проектом** (см. `README.md`)
9. **Настройте мониторинг** (см. `ARCHITECTURE_DOCKER.md`)

---

## 🔧 Управление системой

### Основные команды:

```bash
# Запустить
docker-compose up -d

# Остановить
docker-compose down

# Перезапустить
docker-compose restart

# Логи
docker-compose logs -f

# Статус
docker-compose ps

# Мониторинг ресурсов
docker stats
```

### Backup и Restore:

```bash
# Backup
docker-compose exec postgres pg_dump -U calendar_user calendar_db > backup.sql

# Restore
cat backup.sql | docker-compose exec -T postgres psql -U calendar_user calendar_db
```

### Обновление:

```bash
# Через git
git pull
docker-compose down
docker-compose build --no-cache
docker-compose up -d
```

📖 **Все команды:** `DEPLOY_CHEATSHEET.md`

---

## 📖 Навигация по документации

| Документ | Когда использовать |
|----------|-------------------|
| **START_HERE.md** | Первый запуск, обзор |
| **DEPLOY_CHEATSHEET.md** | Быстрая справка по командам |
| **DEPLOYMENT_CHECKLIST.md** | Пошаговое развертывание |
| **QUICK_START_DOCKER.md** | Краткая инструкция (5 мин) |
| **TRANSFER_TO_SERVER.md** | Способы переноса проекта |
| **GIT_WORKFLOW.md** | 🆕 Разработка и деплой через Git |
| **DOCKER_DEPLOYMENT.md** | Полное руководство |
| **ARCHITECTURE_DOCKER.md** | Понимание архитектуры |
| **DOCKER_README.md** | Технический обзор |

---

## 🆘 Помощь и troubleshooting

### Частые проблемы:

**❌ Порт 3000 занят**
```bash
# Изменить в docker-compose.yml
ports:
  - "8080:3000"
```

**❌ База не инициализируется**
```bash
docker-compose down -v
docker-compose up -d
```

**❌ Нет доступа к Docker**
```bash
sudo usermod -aG docker $USER
# Выйти и войти снова
```

**❌ Недостаточно памяти**
```bash
# Добавить swap
sudo fallocate -l 2G /swapfile
sudo chmod 600 /swapfile
sudo mkswap /swapfile
sudo swapon /swapfile
```

📖 **Полный troubleshooting:** `DEPLOYMENT_CHECKLIST.md` (раздел Troubleshooting)

---

## 📊 Что развернуто?

```
Ваш сервер
│
├── calendar-app (Next.js)      Port: 3000
├── calendar-db (PostgreSQL)    Port: 5433
└── calendar-cron (Alpine)      Cron: */15 min

Сеть: calendar-network (изолированная)
Volume: calendar-db-data (персистентный)
```

**Размер:**
- Docker образы: ~1.2 GB
- База данных: ~50-200 MB (растет)
- RAM usage: ~500-800 MB

📖 **Детальная архитектура:** `ARCHITECTURE_DOCKER.md`

---

## ✅ Чеклист успешного развертывания

- [ ] Docker установлен на сервере
- [ ] Проект перенесен на сервер
- [ ] Файл .env создан и настроен
- [ ] Все пароли изменены
- [ ] `docker-compose up -d` выполнено
- [ ] Все 3 контейнера в статусе "Up"
- [ ] Администратор создан
- [ ] Успешный вход в систему
- [ ] Firewall настроен
- [ ] Backup настроен

📖 **Полный чеклист:** `DEPLOYMENT_CHECKLIST.md`

---

## 💡 Полезные ссылки

**После развертывания:**
- Вход: `http://your-server:3000/login`
- Календарь: `http://your-server:3000/calendar`
- API документация: см. `README.md`

**Endpoints:**
- Прием статей: `POST /api/articles/receive`
- Создание админа: `GET /api/reset-admin`
- Cron публикация: `GET /api/cron/publish`

---

## 🔐 Безопасность

**Обязательно:**
- ✅ Измените все пароли в .env
- ✅ Смените пароль админа после первого входа
- ✅ Настройте firewall
- ✅ Используйте HTTPS (через Nginx + Let's Encrypt)
- ✅ Регулярный backup БД

**Рекомендуется:**
- SSH ключи вместо паролей
- Fail2ban для защиты от брутфорса
- Мониторинг логов
- Регулярные обновления системы

---

## 📞 Поддержка

Если возникли вопросы:

1. Проверьте логи: `docker-compose logs -f`
2. Смотрите раздел Troubleshooting в `DEPLOYMENT_CHECKLIST.md`
3. Проверьте `DOCKER_DEPLOYMENT.md` для детальных инструкций
4. Изучите архитектуру в `ARCHITECTURE_DOCKER.md`

---

## 🎯 Итого

**Что вы получили:**
- ✅ Полностью готовую Docker конфигурацию
- ✅ 13 файлов документации и скриптов
- ✅ Оптимизированный multi-stage Dockerfile
- ✅ Docker Compose с 3 сервисами
- ✅ Автоматическую инициализацию БД
- ✅ Cron для автопубликации
- ✅ Полную документацию на русском языке

**Время развертывания:**
- Опытный пользователь: 5-10 минут
- Новичок с инструкциями: 15-30 минут

**Стоимость:**
- Docker и PostgreSQL: **БЕСПЛАТНО**
- Оплачивается только сервер (который у вас уже есть)

---

**🚀 Удачного развертывания!**

*Начните с шага 1 выше или откройте `QUICK_START_DOCKER.md` для сокращенной версии.*
