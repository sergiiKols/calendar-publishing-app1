# 🐛 Руководство по отладке проблем

## Текущие проблемы

### 1. Кнопка "Выберите проект" не показывает проекты
### 2. Модальное окно Schedule - кнопка OK не сохраняет событие

## Что было сделано

### ✅ Добавлено подробное логирование

Теперь все критические точки имеют консольные логи:

#### В `ProjectSelector.tsx`:
- 🔍 Начало загрузки проектов
- 📡 Статус HTTP ответа
- 📦 Полученные данные
- ✅ Количество загруженных проектов
- 🎯 Автовыбор первого проекта
- ❌ Ошибки при загрузке

#### В `ScheduleModal.tsx`:
- 📝 Отправка формы
- ⚠️ Ошибки валидации
- ✅ Успешная валидация

#### В `calendar/page.tsx`:
- 🚀 Начало обработки расписания
- ❌ Проверки на отсутствие данных
- 📤 Тело запроса к API
- 📥 Ответ от сервера
- ✅ Успешное создание события

#### В `api/projects/route.ts`:
- 🔍 Получение запроса
- 👤 Информация о сессии
- 🆔 ID пользователя
- 📦 Загруженные проекты
- ❌ Ошибки

## Как тестировать

### 1. Откройте консоль браузера
```
F12 → Console
```

### 2. Перейдите на страницу календаря
```
https://calendar-app-gamma-puce.vercel.app/calendar
```

### 3. Проверьте логи при загрузке страницы

Должны появиться логи:
```
🔍 ProjectSelector: Fetching projects...
📡 ProjectSelector: Response status: 200
📦 ProjectSelector: Received data: {...}
✅ ProjectSelector: Projects loaded: 2
🎯 ProjectSelector: Auto-selecting first project: 1
```

### 4. Попробуйте запланировать статью

Кликните на статью в Inbox → заполните форму → нажмите Schedule/OK

Должны появиться логи:
```
📝 ScheduleModal: Form submitted {date: "2026-02-18", time: "17:11", platforms: Array(1)}
✅ ScheduleModal: Validation passed, calling onSubmit
🚀 CalendarPage: handleScheduleSubmit called {...}
📤 Sending request to /api/calendar/events: {...}
📥 Response from server: {status: 201, data: {...}}
✅ Event created successfully
```

## Возможные проблемы и решения

### Проблема: "Unauthorized" при загрузке проектов

**Причина:** Не авторизован или сессия истекла

**Решение:**
1. Перейдите на `/login`
2. Войдите с правильными учетными данными
3. Проверьте переменные окружения `NEXTAUTH_SECRET` и `NEXTAUTH_URL`

### Проблема: Проекты не загружаются (пустой массив)

**Причина:** В базе данных нет проектов для текущего пользователя

**Решение:**
1. Создайте проект в SMI системе
2. SMI автоматически синхронизирует его в Calendar
3. Или используйте API напрямую: `POST /api/projects/sync`

### Проблема: Кнопка OK в модальном окне не реагирует

**Проверьте в консоли:**
- Есть ли лог `📝 ScheduleModal: Form submitted`?
- Если нет → проблема с формой/валидацией
- Если есть → проверьте следующий лог `⚠️ ScheduleModal: Validation failed`

**Возможные причины:**
1. Не заполнена дата
2. Не заполнено время
3. Не выбраны платформы

### Проблема: Event не создается (ошибка 400/500)

**Смотрите лог сервера:**
```
📥 Response from server: {status: 400, data: {error: "..."}}
```

**Частые причины:**
- `Missing required fields` → не все поля переданы
- `No users found in database` → нужно создать admin через `/api/init-setup`
- `Failed to create event` → проблема с БД

## Серверные логи (Vercel)

Для просмотра серверных логов:

1. Откройте Vercel Dashboard
2. Выберите проект `calendar-app`
3. Перейдите в раздел "Logs"
4. Фильтруйте по API routes: `/api/projects`, `/api/calendar/events`

## Проверка интеграции с SMI

### 1. Проверьте проекты в Calendar

```bash
# В консоли браузера
fetch('/api/projects')
  .then(r => r.json())
  .then(console.log)
```

Должен вернуть список проектов с `external_project_id`

### 2. Проверьте синхронизацию из SMI

В SMI проекте при создании проекта должны быть логи:
```
🔄 Syncing project to Calendar...
✅ Project synced: {name: "...", id: ...}
```

### 3. Ручная синхронизация проекта

```bash
curl -X POST https://calendar-app-gamma-puce.vercel.app/api/projects/sync \
  -H "Content-Type: application/json" \
  -d '{
    "external_project_id": 1,
    "name": "Test Project",
    "description": "Test description",
    "color": "#3B82F6"
  }'
```

## Следующие шаги

После деплоя с логированием:

1. ✅ Откройте консоль браузера
2. ✅ Перейдите на `/calendar`
3. ✅ Скопируйте все логи
4. ✅ Найдите где происходит сбой
5. ✅ Отправьте логи для анализа

## Полезные ссылки

- **Production App**: https://calendar-app-gamma-puce.vercel.app/calendar
- **API Projects**: https://calendar-app-gamma-puce.vercel.app/api/projects
- **API Events**: https://calendar-app-gamma-puce.vercel.app/api/calendar/events
- **Vercel Dashboard**: https://vercel.com/dashboard

---

**Дата создания:** 2026-02-16  
**Версия:** 1.0
