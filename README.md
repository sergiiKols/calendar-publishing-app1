# 📅 Calendar Publishing App

Автоматическая система планирования и публикации контента в социальные сети и блоги через календарь.

## 🎯 Возможности

- ✅ Приём статей из SMI проекта через API
- ✅ Визуальный календарь с drag & drop планированием
- ✅ Автоматическая публикация по расписанию (Vercel Cron)
- ✅ Поддержка платформ: WordPress, Telegram, Facebook, Instagram, LinkedIn
- ✅ Управление несколькими аккаунтами
- ✅ Безопасное хранение токенов (шифрование)
- ✅ Фильтры и статусы статей

## 🚀 Быстрый старт

### 1. Установка зависимостей

```bash
npm install
```

### 2. Настройка переменных окружения

```bash
cp .env.example .env.local
```

Отредактируйте `.env.local` и заполните все необходимые переменные.

### 3. Инициализация базы данных

```bash
npm run db:push
```

Это создаст необходимые таблицы в Vercel Postgres.

### 4. Запуск в режиме разработки

```bash
npm run dev
```

Приложение откроется на `http://localhost:3000`

## 📦 Deploy на Vercel

### Шаг 1: Создать проект на Vercel

```bash
vercel
```

### Шаг 2: Добавить Vercel Postgres

1. В Vercel Dashboard → Storage → Create Database
2. Выбрать Postgres
3. Скопировать переменные окружения в Settings → Environment Variables

### Шаг 3: Настроить Environment Variables

В Vercel добавить все переменные из `.env.example`:

```
NEXTAUTH_URL=https://your-app.vercel.app
NEXTAUTH_SECRET=...
CALENDAR_API_KEY=...
POSTGRES_URL=...
# и так далее
```

### Шаг 4: Настроить Vercel Cron

В `vercel.json` уже настроен cron job для автопубликации каждые 15 минут.

Проверьте в Vercel Dashboard → Cron Jobs → должен появиться job.

### Шаг 5: Deploy

```bash
vercel --prod
```

## 🔗 Интеграция с SMI проектом

### В SMI проекте добавить endpoint для отправки статей:

```python
# api_server.py (SMI project)

import requests

@app.post("/api/articles/send-to-calendar")
async def send_to_calendar(article_id: int):
    article = get_article(article_id)
    
    response = requests.post(
        "https://your-calendar-app.vercel.app/api/articles/receive",
        headers={
            "X-API-Key": "your-calendar-api-key"
        },
        json={
            "title": article["title"],
            "content": article["content"],
            "images": article["images"],
            "source_project": "smi_main"
        }
    )
    
    return response.json()
```

## 📊 Структура базы данных

### Таблица: `users`
- Аутентификация пользователей

### Таблица: `inbox_articles`
- Входящие статьи из SMI проекта
- Статусы: inbox, scheduled, published

### Таблица: `calendar_events`
- Запланированные публикации
- Связь с inbox_articles

### Таблица: `publishing_platforms`
- Настройки аккаунтов соцсетей
- Шифрованные токены

### Таблица: `publish_logs`
- История публикаций
- Логи ошибок

## 🔐 Безопасность

- Все пароли и токены **шифруются** перед сохранением в БД
- API endpoints защищены ключами
- Cron jobs защищены секретным ключом
- NextAuth для аутентификации

## 📖 API Documentation

### POST `/api/articles/receive`
Приём статей из SMI проекта

**Headers:**
```
X-API-Key: your-calendar-api-key
```

**Body:**
```json
{
  "title": "Название статьи",
  "content": "Текст статьи",
  "images": ["url1.jpg", "url2.jpg"],
  "source_project": "smi_main"
}
```

### GET `/api/cron/publish`
Автопубликация (вызывается Vercel Cron каждые 15 минут)

**Headers:**
```
X-Cron-Secret: your-cron-secret
```

## 🛠️ Технологии

- **Frontend**: Next.js 14 (App Router), React 18, TailwindCSS
- **Backend**: Next.js API Routes
- **Database**: Vercel Postgres
- **Auth**: NextAuth.js
- **Deployment**: Vercel
- **Cron**: Vercel Cron Jobs

## 📝 TODO

- [ ] Добавить аналитику публикаций
- [ ] A/B тестирование заголовков
- [ ] Массовое планирование
- [ ] Экспорт/импорт календаря

## 📞 Поддержка

Если возникли вопросы - см. документацию в `/docs`

## 📄 Лицензия

Proprietary - SMI Project 2026
