# 🚀 Пошаговый деплой на сервер

## Предварительные требования на сервере

```bash
# Установить Docker и Docker Compose
sudo apt update
sudo apt install -y docker.io docker-compose git
sudo systemctl start docker
sudo systemctl enable docker

# Добавить пользователя в группу docker (чтобы не использовать sudo)
sudo usermod -aG docker $USER
# После этого нужно перелогиниться
```

---

## ШАГ 1: Клонирование проекта с GitHub

```bash
# Перейти в домашнюю директорию или выбрать место для проекта
cd ~

# Клонировать репозиторий
git clone https://github.com/sergiiKols/calendar-publishing-app1.git

# Перейти в директорию проекта
cd calendar-publishing-app1
```

---

## ШАГ 2: Настройка переменных окружения

```bash
# Скопировать пример конфигурации
cp .env.docker.example .env

# Отредактировать .env файл
nano .env
```

### Обязательные переменные в `.env`:

```env
# Database
POSTGRES_USER=calendar_user
POSTGRES_PASSWORD=ВАШ_СЛОЖНЫЙ_ПАРОЛЬ_ДЛЯ_БД
POSTGRES_DB=calendar_db
DATABASE_URL=postgresql://calendar_user:ВАШ_СЛОЖНЫЙ_ПАРОЛЬ_ДЛЯ_БД@db:5432/calendar_db

# NextAuth
NEXTAUTH_URL=http://ВАШ_ДОМЕН_ИЛИ_IP:3000
NEXTAUTH_SECRET=СГЕНЕРИРОВАТЬ_СЛУЧАЙНУЮ_СТРОКУ_32_СИМВОЛА

# SMI Integration
SMI_API_URL=https://api.smi24.uz
SMI_API_TOKEN=ВАШ_SMI_ТОКЕН

# Encryption (для хранения токенов соцсетей)
ENCRYPTION_KEY=СГЕНЕРИРОВАТЬ_СЛУЧАЙНУЮ_СТРОКУ_32_СИМВОЛА

# Admin credentials (первый запуск)
ADMIN_EMAIL=admin@example.com
ADMIN_PASSWORD=ВАШ_ADMIN_ПАРОЛЬ
```

### Генерация секретных ключей:

```bash
# Для NEXTAUTH_SECRET (32 символа)
openssl rand -base64 32

# Для ENCRYPTION_KEY (32 символа)
openssl rand -base64 32
```

---

## ШАГ 3: Создание базы данных и запуск контейнеров

```bash
# Убедиться что находимся в директории проекта
cd ~/calendar-publishing-app1

# Собрать и запустить все контейнеры (app, db, cron)
docker-compose up -d --build
```

### Проверка запуска:

```bash
# Посмотреть статус контейнеров
docker-compose ps

# Должны быть запущены 3 контейнера:
# - calendar-app (приложение Next.js)
# - calendar-db (PostgreSQL)
# - calendar-cron (планировщик публикаций)
```

### Просмотр логов:

```bash
# Все логи
docker-compose logs -f

# Только приложение
docker-compose logs -f app

# Только база данных
docker-compose logs -f db

# Только cron
docker-compose logs -f cron
```

---

## ШАГ 4: Инициализация базы данных

База данных инициализируется автоматически при первом запуске!

### Проверка структуры БД:

```bash
# Подключиться к PostgreSQL
docker-compose exec db psql -U calendar_user -d calendar_db

# Внутри psql выполнить:
\dt    # Показать таблицы
\d users    # Показать структуру таблицы users
\d projects # Показать структуру таблицы projects
\d events   # Показать структуру таблицы events
\q     # Выйти из psql
```

### Создание админ-пользователя:

Админ создается автоматически при первом запуске из переменных `ADMIN_EMAIL` и `ADMIN_PASSWORD`.

Если нужно создать вручную:

```bash
# Выполнить скрипт создания админа
docker-compose exec app node lib/db/create-admin-user.js
```

---

## ШАГ 5: Настройка и проверка Cron

Cron уже запущен в отдельном контейнере! Он автоматически публикует статьи по расписанию.

### Проверка работы Cron:

```bash
# Посмотреть логи cron контейнера
docker-compose logs -f cron

# Вы должны видеть сообщения каждые 5 минут:
# "Checking for scheduled articles..."
```

### Настройка интервала Cron (опционально):

По умолчанию проверка каждые 5 минут. Чтобы изменить:

```bash
# Отредактировать docker-compose.yml
nano docker-compose.yml

# Найти секцию cron и изменить command:
# command: sh -c "while true; do sleep 300; curl -X POST http://app:3000/api/cron/publish; done"
# 300 секунд = 5 минут
# Измените на нужное значение (например, 60 = 1 минута)
```

После изменения:

```bash
# Пересоздать cron контейнер
docker-compose up -d --force-recreate cron
```

---

## ШАГ 6: Проверка доступности приложения

```bash
# Проверить, что приложение отвечает
curl http://localhost:3000

# Или открыть в браузере:
# http://ВАШ_IP_СЕРВЕРА:3000
```

### Первый вход:

1. Откройте `http://ВАШ_IP:3000/login`
2. Введите `ADMIN_EMAIL` и `ADMIN_PASSWORD` из `.env`
3. Вы попадете в календарь публикаций

---

## ШАГ 7: Настройка Nginx (опционально, для продакшн)

Если нужен HTTPS и красивый домен:

```bash
# Установить Nginx
sudo apt install nginx

# Создать конфигурацию
sudo nano /etc/nginx/sites-available/calendar-app
```

Содержимое (пример из `nginx.conf.example`):

```nginx
server {
    listen 80;
    server_name ваш-домен.com;

    location / {
        proxy_pass http://localhost:3000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}
```

Активировать:

```bash
# Создать симлинк
sudo ln -s /etc/nginx/sites-available/calendar-app /etc/nginx/sites-enabled/

# Проверить конфигурацию
sudo nginx -t

# Перезапустить Nginx
sudo systemctl restart nginx
```

### Настройка SSL (Let's Encrypt):

```bash
# Установить certbot
sudo apt install certbot python3-certbot-nginx

# Получить сертификат
sudo certbot --nginx -d ваш-домен.com

# Сертификат будет автоматически обновляться
```

---

## 🔧 Полезные команды

### Управление контейнерами:

```bash
# Остановить все
docker-compose down

# Запустить все
docker-compose up -d

# Перезапустить все
docker-compose restart

# Пересобрать и запустить (после изменений кода)
docker-compose up -d --build

# Удалить все контейнеры и volumes (ОСТОРОЖНО! Удалит БД)
docker-compose down -v
```

### Резервное копирование БД:

```bash
# Создать бэкап
docker-compose exec db pg_dump -U calendar_user calendar_db > backup_$(date +%Y%m%d_%H%M%S).sql

# Восстановить из бэкапа
docker-compose exec -T db psql -U calendar_user calendar_db < backup_20240224_120000.sql
```

### Просмотр логов:

```bash
# Последние 100 строк
docker-compose logs --tail=100 app

# В реальном времени
docker-compose logs -f app

# Все сервисы
docker-compose logs -f
```

### Обновление проекта:

```bash
# Получить последние изменения
git pull origin main

# Пересобрать и запустить
docker-compose up -d --build

# Проверить логи
docker-compose logs -f app
```

---

## ✅ Чеклист успешного деплоя

- [ ] Docker и Docker Compose установлены
- [ ] Проект клонирован с GitHub
- [ ] `.env` файл настроен с правильными паролями и ключами
- [ ] `docker-compose up -d --build` выполнен успешно
- [ ] Все 3 контейнера запущены (`docker-compose ps`)
- [ ] База данных инициализирована
- [ ] Админ-пользователь создан
- [ ] Приложение доступно по http://IP:3000
- [ ] Вход в систему работает
- [ ] Cron работает (видны логи проверки)
- [ ] (Опционально) Nginx настроен
- [ ] (Опционально) SSL сертификат установлен

---

## 🆘 Решение проблем

### Контейнер не запускается:

```bash
# Посмотреть логи ошибок
docker-compose logs app

# Проверить, что порт 3000 свободен
sudo netstat -tulpn | grep 3000
```

### База данных не подключается:

```bash
# Проверить что контейнер db запущен
docker-compose ps db

# Проверить логи базы
docker-compose logs db

# Проверить DATABASE_URL в .env
cat .env | grep DATABASE_URL
```

### Cron не работает:

```bash
# Проверить логи cron
docker-compose logs cron

# Перезапустить cron
docker-compose restart cron

# Проверить что app контейнер доступен
docker-compose exec cron curl -X POST http://app:3000/api/cron/publish
```

---

## 📞 Поддержка

При возникновении проблем:
1. Проверьте логи: `docker-compose logs -f`
2. Убедитесь что все переменные в `.env` заданы правильно
3. Проверьте что порты не заняты другими приложениями
4. Попробуйте пересобрать: `docker-compose down && docker-compose up -d --build`
