# 🐳 Гид по проблемам с кешем Docker при деплое

## 🔴 Проблема: Изменения не появляются после деплоя

### Симптомы:
- ✅ Код изменен и закоммичен в Git
- ✅ Деплой в Dokploy завершился успешно (Done ✅)
- ✅ Backend работает с новыми изменениями
- ❌ **Но фронтенд НЕ обновился** - старая версия компонентов

### Как распознать:
1. Откройте DevTools (F12) → вкладка Network
2. Обновите страницу
3. Найдите файл `page-XXXXXXXXX.js`
4. **Если хеш файла НЕ изменился** - это проблема кеша!

---

## 🔍 Причины проблемы

### 1. **Docker использует закешированные слои**

Docker кеширует каждый шаг (layer) в Dockerfile. Если код не изменился, Docker не пересобирает этот слой.

**Проблема:**
```dockerfile
COPY . .                    # Копирует код
RUN npm run build          # Собирает Next.js
```

Если Git коммит изменил только 1 файл, Docker может решить что:
- "Код скопирован ✅ (есть в кеше)"
- "Build выполнен ✅ (есть в кеше)"
- Результат: **старая сборка .next используется!**

### 2. **Next.js кеширует сборку в `.next` директории**

Next.js сохраняет результаты сборки в `.next/`:
- Скомпилированные страницы
- Оптимизированный JavaScript
- CSS файлы

Если Docker использует старый слой, он копирует **старую .next** директорию!

### 3. **Dokploy может не очистить кеш автоматически**

Даже если вы включили "Clean Cache" в Dokploy, он может очищать только:
- Кеш npm/node_modules
- Но НЕ Docker build cache!

---

## ✅ Решения

### Решение 1: CACHEBUST в Dockerfile (текущее)

**Что это:**
Специальная переменная которая **должна меняться** при каждом деплое.

**Как работает:**
```dockerfile
# Старое значение
ARG CACHEBUST=2026-02-28-SEO-V2

# После изменения на новое
ARG CACHEBUST=2026-03-01-REGION-SELECTION

RUN echo "Cache bust: $CACHEBUST"
COPY . .
RUN npm run build
```

Когда `CACHEBUST` меняется, Docker видит **новое значение** и:
- ❌ Не может использовать кеш для этого слоя
- ✅ Выполняет `COPY . .` заново
- ✅ Выполняет `npm run build` заново

**Проблема:** Нужно **вручную** менять значение при каждом важном деплое!

---

### Решение 2: Git Hook для автоматического обновления CACHEBUST

**Создаем автоматизацию:**

#### Файл: `.git/hooks/pre-commit`

```bash
#!/bin/bash

# Автоматическое обновление CACHEBUST в Dockerfile перед коммитом

DOCKERFILE="./Dockerfile"
CURRENT_DATE=$(date +%Y-%m-%d-%H%M%S)

# Проверяем изменены ли важные файлы
CHANGED_FILES=$(git diff --cached --name-only)

# Проверяем были ли изменения в компонентах, API или lib
if echo "$CHANGED_FILES" | grep -qE "(components/|app/|lib/)"; then
    echo "🔄 Обнаружены изменения в коде, обновляем CACHEBUST..."
    
    # Обновляем CACHEBUST в Dockerfile
    sed -i "s/^ARG CACHEBUST=.*/ARG CACHEBUST=$CURRENT_DATE/" "$DOCKERFILE"
    
    # Добавляем измененный Dockerfile в staging
    git add "$DOCKERFILE"
    
    echo "✅ CACHEBUST обновлен на: $CURRENT_DATE"
else
    echo "ℹ️  Изменения не затрагивают код приложения, CACHEBUST не обновлен"
fi

exit 0
```

**Установка:**
```bash
# В корне проекта
chmod +x .git/hooks/pre-commit
```

**Теперь при каждом коммите:**
- Если изменены компоненты/API/lib → CACHEBUST обновится автоматически!
- Если изменена только документация → CACHEBUST не трогаем (экономим время сборки)

---

### Решение 3: Улучшенный Dockerfile с принудительной очисткой

**Обновленный Dockerfile:**

```dockerfile
FROM base AS builder
WORKDIR /app

COPY --from=deps /app/node_modules ./node_modules

# Принудительно удаляем старую .next директорию
RUN rm -rf .next

# Cache buster - автоматически обновляется через Git Hook
ARG CACHEBUST=2026-03-01-AUTO
RUN echo "Cache bust: $CACHEBUST"

COPY . .

# Очищаем кеш Next.js перед сборкой
RUN rm -rf .next

# Build Next.js application
RUN npm run build
```

**Преимущества:**
- `rm -rf .next` гарантирует чистую сборку
- Даже если Docker использует кеш, старая .next будет удалена

---

## 🎯 Рекомендуемая настройка (комбо всех решений)

### 1. Установите Git Hook

Создайте файл `.git/hooks/pre-commit`:

```bash
#!/bin/bash
DOCKERFILE="./Dockerfile"
CURRENT_DATE=$(date +%Y-%m-%d-%H%M%S)
CHANGED_FILES=$(git diff --cached --name-only)

if echo "$CHANGED_FILES" | grep -qE "(components/|app/|lib/|pages/)"; then
    echo "🔄 Обновляем CACHEBUST: $CURRENT_DATE"
    sed -i "s/^ARG CACHEBUST=.*/ARG CACHEBUST=$CURRENT_DATE/" "$DOCKERFILE"
    git add "$DOCKERFILE"
    echo "✅ CACHEBUST обновлен автоматически"
fi
```

Сделайте исполняемым:
```bash
chmod +x .git/hooks/pre-commit
```

### 2. Обновите Dockerfile

Добавьте принудительную очистку:

```dockerfile
FROM base AS builder
WORKDIR /app
COPY --from=deps /app/node_modules ./node_modules

# Очистка старого кеша
RUN rm -rf .next

# Auto-updated by Git Hook
ARG CACHEBUST=AUTO
RUN echo "Cache bust: $CACHEBUST"

COPY . .

# Еще одна очистка перед сборкой (на всякий случай)
RUN rm -rf .next

# Build
RUN npm run build
```

### 3. В Dokploy включите "Clean Cache"

В настройках приложения:
- ✅ Clean Cache = ON

---

## 🚨 Экстренная очистка кеша

Если проблема все еще есть после всех мер:

### Вариант 1: Через Dokploy UI
1. Вкладка "General"
2. Кнопка "🔨 Rebuild"
3. Clean Cache = ON
4. Rebuild

### Вариант 2: Через Git (force rebuild)

```bash
# Пустой коммит для форсирования деплоя
git commit --allow-empty -m "force: rebuild cache"
git push origin main
```

### Вариант 3: Изменить CACHEBUST вручную

```bash
# Откройте Dockerfile
# Измените строку:
ARG CACHEBUST=2026-03-01-MANUAL-FIX-001

# Коммит и пуш
git add Dockerfile
git commit -m "fix: force Docker cache invalidation"
git push origin main
```

---

## 📋 Чек-лист диагностики проблемы с кешем

- [ ] **1. Проверить хеш JavaScript файла в DevTools**
  - Открыть F12 → Network → найти `page-*.js`
  - Если хеш не изменился → проблема кеша

- [ ] **2. Проверить деплой в Dokploy**
  - Вкладка Deployments → последний деплой = Done?
  - View → Logs → есть ли строка `npm run build`?

- [ ] **3. Проверить CACHEBUST в Dockerfile**
  - Открыть файл Dockerfile
  - Значение `ARG CACHEBUST=...` изменилось?

- [ ] **4. Проверить настройки Dokploy**
  - General → Build Type = Dockerfile?
  - General → Clean Cache = ON?

- [ ] **5. Очистить кеш браузера**
  - Ctrl+Shift+Delete → Очистить все
  - Или открыть в режиме инкогнито

- [ ] **6. Если ничего не помогло → force rebuild**
  - Изменить CACHEBUST вручную на новое значение
  - Коммит + Пуш

---

## 🎓 Как избежать проблемы в будущем

### ✅ Всегда делайте:

1. **Используйте Git Hook** для автоматического обновления CACHEBUST
2. **Включите Clean Cache** в Dokploy
3. **Проверяйте хеш файлов** в DevTools после деплоя
4. **Используйте `rm -rf .next`** в Dockerfile перед сборкой

### ❌ Никогда не делайте:

1. **Не используйте статический CACHEBUST** (всегда обновляйте)
2. **Не надейтесь только на Dokploy Clean Cache** (он не очищает Docker layers)
3. **Не игнорируйте логи сборки** (проверяйте что `npm run build` выполняется)

---

## 📚 Дополнительная информация

### Полезные команды:

```bash
# Проверить что изменилось в последнем коммите
git show --name-only

# Посмотреть хеш последнего коммита
git log -1 --oneline

# Форсировать пересборку Docker без кеша
docker build --no-cache .

# Очистить весь Docker кеш (локально)
docker builder prune -af
```

### Ссылки:
- [Docker Multi-stage builds](https://docs.docker.com/build/building/multi-stage/)
- [Next.js Caching](https://nextjs.org/docs/app/building-your-application/caching)
- [Git Hooks](https://git-scm.com/book/en/v2/Customizing-Git-Git-Hooks)

---

**Создано:** 2026-03-01  
**Версия:** 1.0  
**Автор:** Rovo Dev AI Assistant
