# 🔧 Исправление домена в Dokploy для calendar.energo-audit.online

## 📊 Текущая ситуация (по скриншоту):
- ✅ Домен: `https://calendar.energo-audit.online`
- ✅ DNS Valid (зеленая галочка)
- ✅ Port: 3000
- ❌ **Cert: none** - НЕТ SSL СЕРТИФИКАТА!

## 🎯 Проблема:
Вы настроили домен на **HTTPS**, но SSL сертификат не установлен (`Cert: none`).
Dokploy не может открыть сайт по HTTPS без сертификата.

---

## ✅ РЕШЕНИЕ (3 варианта):

### Вариант 1: Автоматический SSL через Dokploy (РЕКОМЕНДУЕТСЯ)

1. **В интерфейсе Dokploy, во вкладке Domains:**
   - Нажмите на иконку **редактирования** (карандаш) возле домена
   - Найдите опцию **"Generate SSL Certificate"** или **"Let's Encrypt"**
   - Включите автоматическое получение SSL
   - Нажмите **Save/Update**
   - Подождите 1-2 минуты

2. **После этого в списке должно появиться:**
   - `Cert: Let's Encrypt` (или подобное)
   - Статус должен стать зеленым

3. **Передеплойте приложение:**
   - Перейдите во вкладку **Deployments**
   - Нажмите **Redeploy** или **Deploy**

---

### Вариант 2: Временно использовать HTTP (без HTTPS)

Если SSL не настраивается, попробуйте временно HTTP:

1. **Во вкладке Domains:**
   - Измените домен с `https://calendar.energo-audit.online` на `http://calendar.energo-audit.online`
   - Сохраните

2. **Во вкладке Environment:**
   - Измените `NEXTAUTH_URL` на `http://calendar.energo-audit.online`
   - Сохраните

3. **Передеплойте приложение**

⚠️ **Внимание:** HTTP небезопасен для production, но подойдет для тестирования.

---

### Вариант 3: Проверить Traefik (если используется)

Dokploy обычно использует Traefik для управления доменами и SSL.

1. **Проверьте что Traefik запущен:**
   - В Dokploy перейдите в раздел настроек/серверов
   - Убедитесь что Traefik работает

2. **Проверьте labels для контейнера:**
   Dokploy должен автоматически добавить labels для Traefik.
   Если нет, попробуйте пересоздать домен:
   - Удалите домен во вкладке Domains
   - Добавьте заново `calendar.energo-audit.online`
   - Включите SSL/HTTPS
   - Сохраните и передеплойте

---

## 🔍 Диагностика проблемы

### Проверка 1: DNS правильно настроен?
Откройте терминал на вашем компьютере:
```bash
nslookup calendar.energo-audit.online
```

Должен вернуть IP вашего сервера. ✅ У вас DNS Valid, так что это OK.

### Проверка 2: Порт 80 и 443 открыты?
На сервере должны быть открыты порты:
- **80** (HTTP) - для проверки Let's Encrypt
- **443** (HTTPS) - для SSL соединений

### Проверка 3: Приложение работает внутри Docker?
В Dokploy:
- Перейдите во вкладку **Preview Deployments** или **Logs**
- Проверьте что контейнер запущен и нет ошибок
- Проверьте что порт 3000 слушается

---

## 📋 Пошаговая инструкция (что делать СЕЙЧАС):

### Шаг 1: Включить автоматический SSL
1. Откройте **Domains** в Dokploy
2. Нажмите на **иконку редактирования** (карандаш) возле `https://calendar.energo-audit.online`
3. Найдите настройку **SSL Certificate** или **Generate Certificate**
4. Выберите **Let's Encrypt** (если есть опция)
5. Сохраните

### Шаг 2: Проверить Environment переменные
1. Откройте вкладку **Environment**
2. Убедитесь что есть:
   ```
   NEXTAUTH_URL=https://calendar.energo-audit.online
   ```
3. Если нет - добавьте или измените
4. Сохраните

### Шаг 3: Передеплоить
1. Перейдите во вкладку **Deployments**
2. Нажмите **Redeploy** или **Deploy**
3. Дождитесь завершения деплоя (1-3 минуты)

### Шаг 4: Проверить статус SSL
1. Вернитесь во вкладку **Domains**
2. Проверьте что `Cert:` изменился с `none` на `Let's Encrypt` или `Valid`
3. Если все ОК - домен готов!

### Шаг 5: Открыть сайт
1. Откройте браузер
2. Перейдите на `https://calendar.energo-audit.online`
3. Должен открыться ваш Calendar App

---

## 🚨 Если всё равно не работает:

### Проблема: "Cert: none" не меняется

**Причина:** Dokploy не может получить SSL сертификат от Let's Encrypt.

**Решения:**
1. Проверьте что порт 80 открыт (Let's Encrypt требует его для проверки)
2. Попробуйте удалить и пересоздать домен
3. Проверьте логи Traefik (если есть доступ)

### Проблема: "502 Bad Gateway"

**Причина:** Nginx/Traefik не может подключиться к контейнеру на порту 3000.

**Решения:**
1. Проверьте что контейнер запущен (вкладка Preview Deployments)
2. Проверьте логи приложения на ошибки
3. Убедитесь что порт в домене указан правильно (3000)

### Проблема: "Connection timeout"

**Причина:** Firewall блокирует порты 80/443.

**Решения:**
1. Откройте порты в настройках сервера/облака
2. Если используете UFW:
   ```bash
   sudo ufw allow 80
   sudo ufw allow 443
   ```

---

## 📸 Скриншоты что должно быть:

### В Domains после настройки SSL:
```
https://calendar.energo-audit.online
Path: /    Port: 3000    HTTP    Cert: Let's Encrypt    ✅ DNS Valid
```

### В Environment:
```
NEXTAUTH_URL=https://calendar.energo-audit.online
NEXTAUTH_SECRET=ваш_секрет
CALENDAR_API_KEY=ваш_ключ
...
```

---

## ✅ Итоговый чеклист:

- [ ] Домен настроен: `https://calendar.energo-audit.online`
- [ ] SSL сертификат получен (Cert: Let's Encrypt)
- [ ] `NEXTAUTH_URL=https://calendar.energo-audit.online` в Environment
- [ ] Приложение передеплоено
- [ ] Сайт открывается по HTTPS
- [ ] Можно войти в систему

---

## 💡 Подсказка:

Если Dokploy не может автоматически получить SSL:
1. Используйте временно HTTP (вариант 2)
2. Настройте SSL вручную через Certbot на сервере
3. Или используйте Cloudflare для SSL (бесплатно)

---

**Следующий шаг:** Попробуйте Вариант 1 (автоматический SSL) и пришлите скриншот после изменений!
