# 🎯 Сбор семантического ядра через DataForSEO Labs API

## Профессиональная система сбора семкластера

Автоматизированный процесс сбора семантического ядра: от seed-ключей генерируем 100+ связанных слов, обогащаем метриками, анализируем интент через SERP и кластеризуем по семантике с помощью NLP.

---

## 📋 Возможности

### ✅ Что реализовано

1. **Labs API Integration**
   - Keywords for Keywords - генерация связанных ключей из seeds
   - Related Keywords - расширение через похожие запросы
   - Keywords for Site - анализ ключей конкурентов

2. **Intent Analysis (SERP)**
   - Анализ топ-10 результатов
   - Определение интента по SERP features (PAA, Shopping, Local Pack)
   - 5 типов интента: Informational, Transactional, Commercial, Navigational, Local

3. **NLP Кластеризация**
   - TF-IDF векторизация ключевых слов
   - DBSCAN алгоритм для группировки
   - Автоматическое определение оптимального количества кластеров

4. **Фильтрация и ранжирование**
   - По объему поиска (min/max)
   - По сложности ключевого слова (KD)
   - По интенту
   - По стоимости клика (CPC)

5. **Экспорт**
   - CSV с полными данными
   - Группировка по кластерам
   - Метрики на уровне кластера

---

## 🚀 Быстрый старт

### Шаг 1: Применить миграции БД

```bash
# Базовая миграция (если еще не применена)
docker exec -i calendar-db psql -U calendar_user -d calendar_db < lib/db/migrate-add-seo-keywords.sql

# Миграция для семантических кластеров
docker exec -i calendar-db psql -U calendar_user -d calendar_db < lib/db/migrate-add-semantic-clusters.sql
```

### Шаг 2: Проверить credentials

Убедитесь что в `.env` есть:

```bash
DATAFORSEO_LOGIN=your_email@example.com
DATAFORSEO_PASSWORD=your_api_password
```

### Шаг 3: Открыть страницу

```
https://calendar.energo-audit.online/seo
```

### Шаг 4: Собрать семантическое ядро

1. Нажмите кнопку **"Собрать семядро"** (новая кнопка)
2. Введите 1-5 seed-ключевых слов
3. Выберите регион и язык
4. Установите целевой размер (по умолчанию 100)
5. (Опционально) Добавьте домен конкурента
6. Нажмите **"Собрать семядро"**

Подождите 2-5 минут. Система:
- Соберет связанные ключевые слова через Labs API
- Определит интент через SERP анализ
- Сгруппирует по семантике с помощью NLP
- Сохранит всё в БД

---

## 📊 Workflow сбора семантического ядра

### Этап 1: Генерация идей (Labs API)

**Источник 1: Keywords for Keywords**
```
Input: ["python автоматизация"]
Output: ~50-200 связанных ключей
```

Возвращает:
- search_volume
- keyword_difficulty (KD)
- CPC
- competition

**Источник 2: Related Keywords**
```
Input: "python автоматизация"
Output: ~30-50 похожих ключей
```

**Источник 3: Keywords for Site** (если указан домен конкурента)
```
Input: "competitor.com"
Output: ~50-100 ключей конкурента
```

### Этап 2: Обогащение метриками

Все ключевые слова содержат:
- **search_volume** - объем поиска (запросов/месяц)
- **cpc** - стоимость клика в USD
- **competition** - уровень конкуренции (0.00 - 1.00)
- **keyword_difficulty** - сложность продвижения (0-100)

### Этап 3: Анализ интента (SERP API)

Для топ-20 ключевых слов (по search volume) анализируем SERP:

**Факторы анализа:**
- Наличие PAA (People Also Ask) → Informational
- Наличие Shopping Results → Transactional
- Наличие Local Pack → Local
- Слова в заголовках ("buy", "best", "how to")

**5 типов интента:**
1. **Informational** - "как", "что такое", "гайд"
2. **Transactional** - "купить", "цена", "заказать"
3. **Commercial** - "лучший", "обзор", "сравнение"
4. **Navigational** - бренд, "вход", "официальный"
5. **Local** - "рядом", "в городе", "адрес"

### Этап 4: NLP Кластеризация

**Алгоритм:**
1. TF-IDF векторизация всех ключевых слов
2. Вычисление cosine similarity между векторами
3. DBSCAN кластеризация (eps=0.7, min_pts=3)
4. Группировка по семантическому сходству

**Результат:**
- 5-20 кластеров
- Каждый кластер имеет доминирующий intent
- Название кластера = самое популярное ключевое слово

### Этап 5: Фильтрация и сохранение

Применяются фильтры:
- `min_search_volume >= 10`
- `keyword_difficulty <= 50`
- Custom фильтры пользователя

Сохраняется в БД:
- `seo_semantic_clusters` - основная запись
- `seo_clusters` - группы (кластеры)
- `seo_cluster_keywords` - все ключевые слова с метриками

---

## 🗄️ Структура базы данных

### Таблица: seo_semantic_clusters

Главная запись о сборе семядра.

```sql
id                   SERIAL PRIMARY KEY
user_id              INTEGER
project_id           INTEGER (nullable)
name                 TEXT (seeds joined)
seeds                JSONB (array of seed keywords)
language             VARCHAR(10)
location_code        VARCHAR(50)
location_name        VARCHAR(100)
total_keywords       INTEGER
total_search_volume  BIGINT
cluster_count        INTEGER
status               VARCHAR(20)
competitor_domain    VARCHAR(255) (nullable)
created_at           TIMESTAMP
updated_at           TIMESTAMP
```

### Таблица: seo_clusters

Семантические группы (кластеры).

```sql
id                      SERIAL PRIMARY KEY
semantic_cluster_id     INTEGER
cluster_id              INTEGER (from DBSCAN)
cluster_name            TEXT
dominant_intent         VARCHAR(50)
total_search_volume     BIGINT
avg_keyword_difficulty  INTEGER
keywords_count          INTEGER
created_at              TIMESTAMP
```

### Таблица: seo_cluster_keywords

Ключевые слова внутри кластеров.

```sql
id                   SERIAL PRIMARY KEY
cluster_id           INTEGER
keyword              TEXT
search_volume        INTEGER
cpc                  DECIMAL(10, 2)
competition          DECIMAL(3, 2)
keyword_difficulty   INTEGER
intent               VARCHAR(50)
source               VARCHAR(50) (seed/labs/related/competitor)
created_at           TIMESTAMP
```

---

## 📈 Примеры использования

### Пример 1: Базовый сбор семядра

**Input:**
```
Seeds: ["python автоматизация"]
Region: Russia
Language: ru
Target size: 100
```

**Output:**
```json
{
  "total_keywords": 103,
  "cluster_count": 8,
  "total_search_volume": 45230,
  "clusters": [
    {
      "cluster_name": "python автоматизация",
      "keywords_count": 15,
      "dominant_intent": "informational",
      "total_search_volume": 12450
    },
    {
      "cluster_name": "python для начинающих",
      "keywords_count": 18,
      "dominant_intent": "informational",
      "total_search_volume": 8920
    }
    // ... еще 6 кластеров
  ]
}
```

### Пример 2: С анализом конкурента

**Input:**
```
Seeds: ["seo оптимизация", "продвижение сайта"]
Competitor: "ahrefs.com"
Region: USA
Language: en
Target size: 150
```

**Output:**
- ~150 ключевых слов
- Включает ключи из ahrefs.com
- Группировка по интенту и семантике

### Пример 3: С фильтрами

**Input:**
```
Seeds: ["маркетинг"]
Filters:
  - min_search_volume: 100
  - max_keyword_difficulty: 30
Target size: 100
```

**Output:**
- Только ключи с SV >= 100
- Только с низкой конкуренцией (KD <= 30)
- Лучшие возможности для SEO

---

## 💰 Стоимость

### DataForSEO Labs API Pricing

| Endpoint | Стоимость за запрос | Примечание |
|----------|---------------------|------------|
| Labs Keywords for Keywords | $0.001 | За каждый seed |
| Labs Related Keywords | $0.001 | За каждый seed |
| Labs Keywords for Site | $0.005 | За домен |
| SERP Advanced | $0.002 | За ключевое слово |

### Примерная стоимость сбора семядра

**Базовый (без SERP анализа):**
- 3 seeds
- Keywords for Keywords: 3 × $0.001 = $0.003
- Related Keywords: 3 × $0.001 = $0.003
- **Итого: ~$0.01**

**С SERP анализом (топ-20):**
- Базовый: $0.01
- SERP для 20 ключей: 20 × $0.002 = $0.04
- **Итого: ~$0.05**

**С конкурентом:**
- Базовый: $0.01
- Keywords for Site: $0.005
- SERP: $0.04
- **Итого: ~$0.055**

**Рекомендация:** Для тестирования достаточно $10 на балансе DataForSEO.

---

## 🎨 UI Компоненты

### SemanticClusterForm

Форма для сбора семядра:
- Ввод 1-5 seed-ключей
- Выбор региона и языка
- Настройка размера кластера
- Опциональный домен конкурента
- Фильтры качества

### Просмотр результатов

После сбора доступно:
- Список всех кластеров
- Детальный просмотр каждого кластера
- Ключевые слова с метриками
- Экспорт в CSV

---

## 📥 Экспорт данных

### CSV формат

```csv
Cluster ID,Cluster Name,Dominant Intent,Cluster Search Volume,Cluster Difficulty,Keyword,Search Volume,CPC,Competition,Keyword Difficulty,Intent,Source
1,"python автоматизация",informational,12450,35,"python автоматизация",2400,1.20,0.65,35,informational,seed
1,"python автоматизация",informational,12450,35,"автоматизация python примеры",890,0.80,0.45,28,informational,labs
...
```

### Использование CSV

1. Импорт в Excel/Google Sheets
2. Анализ в Python/R
3. Загрузка в инструменты SEO
4. Планирование контент-стратегии

---

## 🔧 API Endpoints

### POST /api/seo/semantic-cluster

Создание семантического кластера.

**Request:**
```json
{
  "seeds": ["keyword1", "keyword2"],
  "language": "en",
  "location_code": "2840",
  "location_name": "United States",
  "target_size": 100,
  "competitor_domain": "example.com",
  "project_id": 1,
  "filters": {
    "min_search_volume": 10,
    "max_keyword_difficulty": 50
  }
}
```

**Response:**
```json
{
  "success": true,
  "cluster_id": 42,
  "summary": {
    "total_keywords": 103,
    "total_found": 245,
    "total_search_volume": 45230,
    "cluster_count": 8,
    "intent_distribution": {
      "informational": 65,
      "transactional": 20,
      "commercial": 15,
      "navigational": 2,
      "local": 1
    },
    "processing_time_ms": 125430
  },
  "clusters": [...]
}
```

### GET /api/seo/semantic-cluster

Список семантических кластеров.

**Query params:**
- `project_id` - фильтр по проекту
- `limit` - ограничение (default: 20)
- `offset` - смещение

### GET /api/seo/semantic-cluster/:id

Детальная информация о кластере со всеми ключевыми словами.

### GET /api/seo/semantic-cluster/:id/export

Экспорт кластера в CSV.

### DELETE /api/seo/semantic-cluster/:id

Удаление кластера и всех связанных данных.

---

## 🧪 Примеры запросов

### cURL: создание семкластера

```bash
curl -X POST https://calendar.energo-audit.online/api/seo/semantic-cluster \
  -H "Content-Type: application/json" \
  -H "Cookie: next-auth.session-token=YOUR_TOKEN" \
  -d '{
    "seeds": ["python automation"],
    "language": "en",
    "location_code": "2840",
    "location_name": "United States",
    "target_size": 100
  }'
```

### JavaScript: получение списка

```javascript
const response = await fetch('/api/seo/semantic-cluster?limit=10');
const data = await response.json();
console.log(data.clusters);
```

### Python: анализ экспортированных данных

```python
import pandas as pd

# Загрузить CSV
df = pd.read_csv('semantic-cluster-python-automation.csv')

# Группировка по интенту
intent_groups = df.groupby('Intent').agg({
    'Search Volume': 'sum',
    'Keyword': 'count'
})

print(intent_groups)
```

---

## 📚 Лучшие практики

### 1. Выбор seed-ключей

✅ **Хорошо:**
- Широкие тематические запросы
- 2-3 слова
- Средняя конкуренция

❌ **Плохо:**
- Слишком узкие ниши
- Только бренды
- 1 слово (слишком широко)

### 2. Настройка фильтров

**Для новых сайтов:**
```
min_search_volume: 50
max_keyword_difficulty: 30
```

**Для established сайтов:**
```
min_search_volume: 500
max_keyword_difficulty: 60
```

### 3. Оптимизация стоимости

- Начните с target_size: 50-100
- Используйте SERP анализ только для топ-20
- Добавляйте конкурента только если нужно

### 4. Использование результатов

1. **Content Strategy**
   - Informational → блог статьи
   - Transactional → landing pages
   - Commercial → сравнения, reviews

2. **PPC Campaigns**
   - Сортировка по CPC
   - Группировка по интенту
   - Ad groups из кластеров

3. **SEO Optimization**
   - Низкий KD + высокий SV = priority
   - Кластеры → pillar pages
   - Long-tail → supporting content

---

## 🐛 Troubleshooting

### Проблема: Мало ключевых слов собрано

**Причины:**
- Слишком строгие фильтры
- Узкая ниша
- Редкий seed-запрос

**Решение:**
- Уменьшите min_search_volume
- Увеличьте max_keyword_difficulty
- Попробуйте другие seeds

### Проблема: Долгая обработка

**Нормально:**
- 100 ключей: 2-3 минуты
- 200 ключей: 4-5 минут

**Если дольше:**
- Проверьте логи сервера
- Проверьте rate limits DataForSEO

### Проблема: Кластеры не создаются

**Причины:**
- Слишком разнородные ключевые слова
- Низкий порог similarity (eps)

**Решение:**
- Используйте более связанные seeds
- Проверьте language code

---

## 🔗 Ссылки

- [DataForSEO Labs API Docs](https://docs.dataforseo.com/v3/dataforseo_labs/)
- [DBSCAN Algorithm](https://en.wikipedia.org/wiki/DBSCAN)
- [TF-IDF](https://en.wikipedia.org/wiki/Tf%E2%80%93idf)
- [Keyword Intent Types](https://ahrefs.com/blog/search-intent/)

---

**Версия:** 2.0.0  
**Дата:** 27 февраля 2026  
**Автор:** Rovo Dev
