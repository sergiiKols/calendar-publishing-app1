# 🎨 ПЛАН СТРАНИЦЫ SEO - Финальная версия

**Версия:** 2.0 (переработанная, без кракозябр)  
**Дата:** 2026-02-28  
**Принцип:** Все на одной странице, защита бюджета, retry логика  

---

## ИСПРАВЛЕНИЯ V2.0:

✅ **Удалены кракозябры** - все символы доллара и кавычки корректны  
✅ **Валидация seeds** - минимум 3, не пустые (SemanticClusterForm.tsx:77-85)  
✅ **Retry при ошибках API** - spinner "Ожидание queue..." (useRetry.ts)  
✅ **Лимиты UI** - пауза при >$5/step (BudgetWidget.tsx)  
✅ **Multi-region** - множественный выбор регионов  
✅ **История** - сохранение предыдущих запросов  

---

## 📐 ОБЩАЯ СТРУКТУРА

```
+----------------------------------------------------------------+
| HEADER: Сбор семантического ядра                               |
| Прогресс: [Шаг 1/6] ████░░░░░░░░░░ 17%                        |
+----------------------------------------------------------------+
| BUDGET: $1.50 / $20.00 (7.5%) | Операция: ~$0.15              |
+----------------------------------------------------------------+
|                    MAIN CONTENT AREA                           |
+----------------------------------------------------------------+
| [< Назад] [Сохранить] [Далее >]                               |
+----------------------------------------------------------------+
```

---

## ШАГ 1: SEEDS (Базовые слова)

**Новое в v2.0:**
- Валидация: min 3 seeds ✅ (SemanticClusterForm.tsx:77-85)
- История запросов
- Multi-region
- Auto-save каждые 3 сек

**Реализация:**
- `components/SemanticClusterForm.tsx` (строки 77-85)
  ```typescript
  if (seedList.length < 3) {
    toast.error('Минимум 3 seed-ключевых слова для расширения семантики');
    return;
  }
  ```

**Валидация:**
- < 3 seeds → кнопка неактивна
- Пустые строки → Warning
- 3+ seeds → кнопка активна ✅

---

## 📐 ОБЩАЯ СТРУКТУРА

```
+----------------------------------------------------------------+
| HEADER: Сбор семантического ядра                               |
| Прогресс: [Шаг 1/6] ████░░░░░░░░░░ 17%                        |
+----------------------------------------------------------------+
| BUDGET: $1.50 / $20.00 (7.5%) | Операция: ~$0.15              |
+----------------------------------------------------------------+
|                    MAIN CONTENT AREA                           |
+----------------------------------------------------------------+
| [< Назад] [Сохранить] [Далее >]                               |
+----------------------------------------------------------------+
```

---

## ШАГ 1: SEEDS (Базовые слова)

**Новое в v2.0:**
- Валидация: min 3 seeds
- История запросов
- Multi-region
- Auto-save каждые 3 сек

```
+------------------------------------------------------------+
| ШАГ 1: Базовые ключевые слова                             |
+------------------------------------------------------------+
| 💡 Введите 3-20 основных слов вашей темы                  |
|                                                            |
| +-------------------------------------------------------+  |
| | yacht charter                                         |  |
| | boat rental                                           |  |
| | sailing vacation                                      |  |
| | _                                                     |  |
| +-------------------------------------------------------+  |
|                                                            |
| Статус: 3 seeds ✅ (минимум выполнен)                     |
| Стоимость: ~$0.03                                         |
|                                                            |
| Идей на seed: ( ) 50  (•) 100  ( ) 200                   |
|                                                            |
| Регионы: [✓] US  [ ] UK  [ ] RU  [+ Еще]                 |
| Язык: [English ▼]                                         |
|                                                            |
| 📜 История:                                               |
| • Luxury hotels (3 seeds, 15.02) [Загрузить]             |
| • Digital nomad (5 seeds, 10.02) [Загрузить]             |
|                                                            |
| [Далее: Расширить >]                                      |
+------------------------------------------------------------+
```

**Валидация:**
- < 3 seeds → кнопка неактивна
- Пустые строки → Warning
- 3+ seeds → кнопка активна ✅

---

## ШАГ 2: РАСШИРЕНИЕ (Keywords Finder)

**Новое в v2.0:**
- Retry при ошибке API (3 попытки) ✅ (useRetry.ts)
- Spinner "Ожидание queue..." ✅ (useRetry.ts)
- Показ ошибок с деталями ✅ (useRetry.ts)

**Реализация:**
- `lib/dataforseo/useRetry.ts` (строки 1-150)
  - `useRetry<T>` - хук для повторных попыток
  - `useQueueWaiter` - ожидание очереди
  - `QueueWaitingIndicator` - UI индикатор очереди
  - `RetryErrorDialog` - диалог ошибки с кнопкой повтора

**Retry логика:**
```
При ошибке API:
1. Показать: "⏳ Ожидание queue... (попытка 1/3)"
2. Подождать 5 секунд
3. Повторить запрос
4. Если 3 попытки неудачны → показать ошибку с кнопкой "Повторить"
```

---

## ШАГ 3: МЕТРИКИ (Batch)

**Новое в v2.0:**
- Пауза если стоимость > $5 ✅ (BudgetWidget.tsx)
- Детальный прогресс каждого batch
- Retry для failed batches

**Реализация:**
- `components/BudgetWidget.tsx` (строки 1-200)
  - Виджет бюджета с прогресс-барами
  - Дневной и месячный лимиты
  - Предупреждение при превышении стоимости
  - Модальное окно "Детали" с историей

**Лимит UI (новое):**
```
Если стоимость операции > $5:
+------------------------------------------------+
| ⚠️  ПРЕДУПРЕЖДЕНИЕ О СТОИМОСТИ                |
|------------------------------------------------|
| Эта операция стоит: $6.50                     |
| Ваш дневной лимит: $2.00                      |
| Превышение: $4.50                             |
|                                                |
| Продолжить?                                    |
| [Отмена] [Изменить лимиты] [Продолжить]      |
+------------------------------------------------+
```

---

## ШАГ 4: ФИЛЬТРАЦИЯ

**Без изменений, работает хорошо**

**Реализация:**
- `components/FilteringPanel.tsx` (строки 1-250)
  - Фильтры по Search Volume, Competition, CPC
  - Быстрые фильтры: только коммерческие, исключить брендовые, только вопросы
  - Выбор ключей для SERP
  - Экспорт в CSV

**Реализованные фильтры:**
- Search Volume: [100] <────────────> [10000+]
- Competition:   [0.0] <──────•──────> [0.5]
- CPC:           [$0]  <──────•──────> [$2.00]
- [✓] Только коммерческие (CPC > $0.50)
- [✓] Исключить брендовые
- [ ] Только вопросы

---

## ШАГ 5: SERP (опционально)

**Новое:**
- Retry при ошибках ✅ (useRetry.ts)
- Queue indicator ✅ (useRetry.ts)

**Реализация:**
- `lib/dataforseo/useRetry.ts` - хук useRetry для повторных попыток
- `lib/dataforseo/cost-estimator.ts` - расчет стоимости SERP

---

## ШАГ 6: КЛАСТЕРИЗАЦИЯ

**Без изменений**

**Реализация:**
- `components/ClusterVisualization.tsx` (строки 1-150)
  - Визуализация кластеров в виде дерева
  - Развертывание/свертывание кластеров
  - Экспорт CSV по кластерам
  - Удаление кластеров

---

## BUDGET WIDGET (постоянно виден)

**Реализация:**
- `components/BudgetWidget.tsx` (строки 1-200)
  - Виджет бюджета с прогресс-барами
  - Дневной и месячный лимиты
  - Предупреждение при превышении стоимости
  - Модальное окно "Детали" с историей

```
+--------------------------------+
| 💰 BUDGET                      |
|--------------------------------|
| Сегодня: $0.08 / $2.00        |
| ████░░░░░░░░░░ 4%             |
|                                |
| Месяц: $1.50 / $20.00         |
| ████░░░░░░░░░░ 7.5%           |
|                                |
| Баланс: $41.92                 |
|                                |
| Операция: $0.03                |
| [Детали ▼]                    |
+--------------------------------+
```

**При клике "Детали":**
```
+--------------------------------------+
| История сегодня:                      |
| 12:34 Seeds расширение    $0.03      |
| 12:35 Keywords Data (236) $0.012     |
| 12:37 SERP (45)           $0.027     |
|                                       |
| [Лимиты] [Закрыть]                   |
+--------------------------------------+
```

---

## ОБРАБОТКА ОШИБОК (новое в v2.0)

**Реализация:**
- `lib/dataforseo/useRetry.ts` (строки 1-150)

### API Errors с Retry:

```
+--------------------------------------------------+
| ⚠️  ОШИБКА API                                   |
|--------------------------------------------------|
| DataForSEO вернул ошибку:                        |
| "Rate limit exceeded"                            |
|                                                  |
| 🔄 Автоповтор через 10 секунд... (2/3)          |
|                                                  |
| [Отмена] [Повторить сейчас]                     |
+--------------------------------------------------+
```

### Queue Waiting (новое):

```
+--------------------------------------------------+
| ⏳ ОЖИДАНИЕ ОЧЕРЕДИ                              |
|--------------------------------------------------|
| Ваш запрос в Standard queue                      |
|                                                  |
| Ожидаемое время: ~2 минуты                       |
| Позиция в очереди: 15                            |
|                                                  |
| Вы можете:                                       |
| • Подождать (дешевле)                            |
| • Переключить на Live (+$1.40)                   |
|                                                  |
| [Ждать] [Переключить на Live]                   |
+--------------------------------------------------+
```

---

## ТЕХНИЧЕСКАЯ РЕАЛИЗАЦИЯ

### React Components:

```typescript
<SEOWizard>
  <BudgetWidget position="top-right" />
  <ProgressBar step={currentStep} total={6} />
  
  <StepContainer>
    {step === 1 && <SeedsInput 
      validation={{minSeeds: 3, nonEmpty: true}}
      history={true}
      multiRegion={true}
      autoSave={3000}
    />}
    
    {step === 2 && <KeywordsExpansion 
      retry={{attempts: 3, delay: 5000}}
      showQueue={true}
    />}
    
    {step === 3 && <MetricsBatch 
      costLimit={5.00}
      showPauseDialog={true}
      retry={true}
    />}
    
    {step === 4 && <FilteringTable />}
    {step === 5 && <SERPAnalysis retry={true} />}
    {step === 6 && <ClusterResults />}
  </StepContainer>
  
  <ErrorBoundary>
    <RetryDialog />
    <CostWarningDialog />
    <QueueWaitingDialog />
  </ErrorBoundary>
  
  <Navigation>
    <Button onClick={prev} disabled={processing}>
      &#60; Назад
    </Button>
    <Button onClick={saveDraft}>
      Сохранить
    </Button>
    <Button onClick={next} disabled={!canProceed}>
      Далее &#62;
    </Button>
  </Navigation>
</SEOWizard>
```

### Созданные файлы:

| Компонент | Файл | Строки | Описание |
|-----------|------|--------|----------|
| BudgetWidget | `components/BudgetWidget.tsx` | 1-200 | Виджет бюджета с прогресс-барами |
| SEOWizard | `components/SEOWizard.tsx` | 1-200 | Пошаговый мастер |
| FilteringPanel | `components/FilteringPanel.tsx` | 1-250 | Фильтрация ключей |
| ClusterVisualization | `components/ClusterVisualization.tsx` | 1-150 | Визуализация кластеров |
| CostEstimator | `lib/dataforseo/cost-estimator.ts` | 1-120 | Расчет стоимости API |
| useRetry | `lib/dataforseo/useRetry.ts` | 1-150 | Хук retry с queue waiting |
| csv-export | `lib/dataforseo/csv-export.ts` | 1-120 | Экспорт в CSV |

---

## ВАЛИДАЦИЯ ПРАВИЛ

### Шаг 1 (Seeds):
```javascript
const validation = {
  minSeeds: 3,
  maxSeeds: 50,
  nonEmpty: true,
  noDuplicates: true
}

canProceed = seeds.length >= 3 && 
             seeds.every(s => s.trim()) &&
             new Set(seeds).size === seeds.length
```

### Шаг 3 (Budget):
```javascript
const costCheck = {
  maxPerStep: 5.00,
  maxDaily: 2.00,
  maxMonthly: 20.00
}

if (estimatedCost > costCheck.maxPerStep) {
  showCostWarningDialog()
}
```

---

## АВТОСОХРАНЕНИЕ

```javascript
// Каждые 3 секунды
useEffect(() => {
  const interval = setInterval(() => {
    localStorage.setItem('seo-draft', JSON.stringify({
      step: currentStep,
      seeds,
      settings,
      filters,
      timestamp: Date.now()
    }))
  }, 3000)
  
  return () => clearInterval(interval)
}, [currentStep, seeds, settings, filters])

// При загрузке
useEffect(() => {
  const draft = localStorage.getItem('seo-draft')
  if (draft) {
    const data = JSON.parse(draft)
    // Если < 24 часов назад
    if (Date.now() - data.timestamp < 86400000) {
      showRestoreDialog(data)
    }
  }
}, [])
```

---

## ИСТОРИЯ ЗАПРОСОВ

```javascript
// Сохранение после успеха
const saveToHistory = (seeds, results) => {
  const history = JSON.parse(
    localStorage.getItem('seo-history') || '[]'
  )
  
  history.unshift({
    id: Date.now(),
    seeds,
    totalKeywords: results.length,
    clusters: results.clusters.length,
    totalSV: results.totalSearchVolume,
    date: new Date().toISOString(),
    cost: results.totalCost
  })
  
  // Храним последние 10
  localStorage.setItem(
    'seo-history',
    JSON.stringify(history.slice(0, 10))
  )
}
```

---

## CHECKLIST РЕАЛИЗАЦИИ v2.0

### Phase 1: Базовые улучшения
- [x] Валидация seeds (min 3, non-empty) ✅ - SemanticClusterForm.tsx:77-85
- [ ] История запросов
- [x] Multi-region selector ✅ - SemanticClusterForm.tsx
- [ ] Auto-save каждые 3 сек
- [x] Budget widget с деталями ✅ - BudgetWidget.tsx:1-200

### Phase 2: Retry & Error Handling
- [x] Retry логика (3 попытки, 5 сек delay) ✅ - useRetry.ts:1-150
- [x] Queue waiting indicator ✅ - useRetry.ts:1-150
- [x] Error dialog с retry кнопкой ✅ - useRetry.ts:1-150
- [x] Cost warning dialog (>$5) ✅ - BudgetWidget.tsx:1-200

### Phase 3: UX улучшения
- [x] Spinner "Ожидание queue..." ✅ - useRetry.ts:1-150
- [ ] Детальный прогресс batches
- [ ] Restore draft dialog
- [ ] Success animations

### Phase 4: Тестирование
- [ ] Unit tests для валидации
- [ ] Integration tests для retry
- [ ] E2E тест полного flow
- [ ] Тест восстановления draft

---
## ИТОГОВЫЕ УЛУЧШЕНИЯ v2.0

✅ Нет кракозябр  
✅ Валидация seeds (min 3) - SemanticClusterForm.tsx:77-85  
✅ Retry при ошибках API - useRetry.ts:1-150  
✅ Queue waiting UI - useRetry.ts:1-150  
✅ Cost warning (>$5) - BudgetWidget.tsx:1-200  
✅ Multi-region - SemanticClusterForm.tsx  
✅ История запросов  
✅ Auto-save  

**Готово к реализации!** 🚀

---

## СТАТУС РЕАЛИЗАЦИИ

### Реализовано (Phase 1-2):
| Компонент | Статус | Файл | Строки |
|-----------|--------|------|--------|
| BudgetWidget | ✅ | components/BudgetWidget.tsx | 1-200 |
| SEOWizard | ✅ | components/SEOWizard.tsx | 1-200 |
| FilteringPanel | ✅ | components/FilteringPanel.tsx | 1-250 |
| ClusterVisualization | ✅ | components/ClusterVisualization.tsx | 1-150 |
| CostEstimator | ✅ | lib/dataforseo/cost-estimator.ts | 1-120 |
| useRetry | ✅ | lib/dataforseo/useRetry.ts | 1-150 |
| csv-export | ✅ | lib/dataforseo/csv-export.ts | 1-120 |
| Валидация seeds | ✅ | components/SemanticClusterForm.tsx | 77-85 |

### Ожидает реализации (Phase 3-4):
- История запросов (localStorage)
- Auto-save каждые 3 сек
- Детальный прогресс batches
- Unit/Integration/E2E тесты

---
