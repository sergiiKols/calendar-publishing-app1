# 🎯 Новая структура SEO модуля

## 📋 Что изменилось

### До:
```
/seo → Одна страница со всеми проектами вместе
- Фильтры по проектам
- Все ключевые слова всех проектов в одной таблице
- Нужно постоянно выбирать проект
```

### После:
```
/seo → Выбор проекта → Рабочая зона проекта

Шаг 1: Сетка проектов
┌────────┬────────┬────────┬─────────┐
│ Проект │ Проект │ Проект │ [+ Новый]│
│   1    │   2    │   3    │          │
│ 10 слов│ 25 слов│ 5 слов │          │
└────────┴────────┴────────┴─────────┘

Шаг 2: Клик на проект → Рабочая зона
┌──────────────────────────────────────┐
│ ← Назад  |  📁 Проект 1              │
│ [+ Слова] [Wizard] [Семядро]         │
│                                      │
│ Статистика проекта                   │
│ Таблица ключевых слов (только этого) │
│ Кластеры (только этого проекта)      │
└──────────────────────────────────────┘
```

---

## 🆕 Новые компоненты

### 1. **ProjectsGrid.tsx**
**Назначение**: Сетка карточек проектов для выбора

**Функции**:
- ✅ Отображение всех проектов в виде карточек
- ✅ Создание нового проекта (кнопка + форма)
- ✅ Редактирование проекта (inline editing)
- ✅ Удаление проекта (с подтверждением)
- ✅ Клик на карточку → открытие рабочей зоны
- ✅ Показ количества ключевых слов на карточке

**Расположение карточек**: 
- Desktop: 4 в ряд
- Tablet: 2-3 в ряд
- Mobile: 1 в ряд

---

### 2. **ProjectWorkspace.tsx**
**Назначение**: Рабочая зона для работы с выбранным проектом

**Функции**:
- ✅ Кнопка "Назад к проектам"
- ✅ Название и описание проекта в header
- ✅ Кнопки действий (Добавить слова, Wizard, Семядро)
- ✅ Статистика проекта
- ✅ Таблица ключевых слов (только этого проекта)
- ✅ Семантические кластеры (только этого проекта)
- ✅ Фильтры (статус, регион, язык)
- ✅ Все модальные окна (QuickForm, Wizard, Results, etc.)

**Автоматическая фильтрация**:
- Все запросы к API включают `project_id`
- Пользователь видит только данные выбранного проекта

---

## 🎨 Новый User Flow

### Сценарий 1: Работа с существующим проектом

```
1. Открыть /seo
   → Видим сетку проектов

2. Кликнуть на проект "Интернет-магазин"
   → Открывается рабочая зона этого проекта
   
3. Нажать "Добавить слова"
   → Форма уже знает, что проект = "Интернет-магазин"
   → Не нужно выбирать проект заново
   
4. Добавить ключевые слова
   → Слова добавляются к этому проекту
   → Таблица обновляется автоматически
   
5. Просмотреть результаты
   → Кнопка 👁️ → модальное окно с данными
   
6. Нажать "← Назад"
   → Возврат к сетке проектов
```

### Сценарий 2: Создание нового проекта

```
1. Открыть /seo
   → Сетка проектов

2. Нажать "[+ Создать проект]"
   → Открывается форма
   
3. Заполнить:
   - Название: "Блог о путешествиях"
   - Описание: "SEO для туристического блога"
   
4. Нажать "Создать"
   → Проект создан
   → Появляется новая карточка в сетке
   
5. Кликнуть на новый проект
   → Открывается пустая рабочая зона
   → Можно сразу добавлять ключевые слова
```

### Сценарий 3: Редактирование/Удаление проекта

```
1. На карточке проекта нажать "Редактировать"
   → Инлайн форма редактирования
   
2. Изменить название/описание
   → Нажать "Сохранить"
   → Изменения применены
   
3. Для удаления нажать "Удалить"
   → Первый клик: "Удалить проект?"
   → Второй клик: Проект удалён
```

---

## 🔄 Изменения в файлах

### Новые файлы:
1. ✨ `components/ProjectsGrid.tsx` (21.5 KB)
2. ✨ `components/ProjectWorkspace.tsx` (13.8 KB)
3. 📄 `NEW_PROJECT_WORKFLOW.md` (этот файл)

### Изменённые файлы:
1. 🔧 `app/(dashboard)/seo/page.tsx`
   - Упрощена логика: только выбор проекта vs рабочая зона
   - Удалены фильтры, stats, таблицы (теперь в ProjectWorkspace)
   - Добавлен state `selectedProject`

### Удалённый функционал из главной страницы:
- ❌ Фильтры по проектам (теперь не нужны)
- ❌ Общая таблица всех ключевых слов
- ❌ Кнопки действий в header (теперь в workspace)
- ❌ Статистика по всем проектам (теперь per-project)

---

## 📊 Структура данных

### API запросы теперь фильтруются по проекту:

```javascript
// В ProjectWorkspace автоматически добавляется project_id
GET /api/seo/keywords?project_id=1
GET /api/seo/semantic-cluster?project_id=1

// Создание также привязывается к проекту
POST /api/seo/keywords
{
  keywords: [...],
  project_id: 1  // Автоматически из выбранного проекта
}
```

---

## 🎯 Преимущества новой структуры

### 1. **Чистота интерфейса**
- Один проект = один экран
- Нет путаницы с данными разных проектов
- Легче ориентироваться

### 2. **Удобство работы**
- Не нужно каждый раз выбирать проект
- Все действия в контексте проекта
- Быстрое переключение между проектами

### 3. **Лучшая производительность**
- Загружаются только данные нужного проекта
- Меньше данных на странице
- Быстрее рендеринг

### 4. **Масштабируемость**
- Легко добавить статистику проекта
- Можно добавить настройки проекта
- Можно добавить экспорт проекта
- Можно добавить архивацию проекта

---

## 🔍 Как это работает

### State management:

```typescript
// app/(dashboard)/seo/page.tsx

const [projects, setProjects] = useState<Project[]>([]);
const [selectedProject, setSelectedProject] = useState<Project | null>(null);

// Переключение между views:
{selectedProject ? (
  <ProjectWorkspace 
    project={selectedProject} 
    onBack={() => setSelectedProject(null)} 
  />
) : (
  <ProjectsGrid 
    projects={projects}
    onSelectProject={(id) => setSelectedProject(projects.find(p => p.id === id))}
  />
)}
```

### Автоматическая фильтрация:

```typescript
// components/ProjectWorkspace.tsx

const fetchKeywords = async () => {
  // Всегда фильтруем по project.id
  const response = await fetch(`/api/seo/keywords?project_id=${project.id}`);
  // ...
};

const handleQuickAdd = () => {
  // preselectedProjectId передаётся в форму
  <QuickKeywordForm 
    preselectedProjectId={project.id}
    // Форма не позволит выбрать другой проект
  />
};
```

---

## 🚀 Миграция для пользователей

### Что делать после обновления:

1. **Откройте /seo**
   - Увидите новую сетку проектов
   - Все существующие проекты на месте

2. **Кликните на проект**
   - Откроется рабочая зона
   - Все ключевые слова и кластеры на месте

3. **Всё как раньше, но удобнее!**
   - Те же функции
   - Тот же функционал
   - Но лучшая организация

---

## 📝 Changelog

### [2.0.0] - 2026-02-28

#### Added
- ProjectsGrid component для управления проектами
- ProjectWorkspace component для работы с проектом
- Карточный интерфейс выбора проектов
- Inline редактирование проектов в сетке
- Счётчик ключевых слов на карточках проектов

#### Changed
- Полная реструктуризация SEO страницы
- Перенесён весь функционал ключевых слов в ProjectWorkspace
- Упрощена навигация: проекты → workspace → назад

#### Removed
- Глобальные фильтры по проектам (не нужны)
- Общая таблица всех ключевых слов всех проектов

#### Migration Guide
- Нет breaking changes в API
- Нет потери данных
- Все функции сохранены
- Просто новый UI/UX

---

## ✅ Тестирование

### Проверено:
- ✅ Создание проекта
- ✅ Редактирование проекта
- ✅ Удаление проекта
- ✅ Переход в рабочую зону
- ✅ Добавление ключевых слов в проект
- ✅ Просмотр результатов
- ✅ Создание семантического кластера
- ✅ Возврат к списку проектов
- ✅ Сборка проекта без ошибок

### Совместимость:
- ✅ Desktop (1920x1080)
- ✅ Tablet (768x1024)
- ✅ Mobile (375x667)

---

## 🎉 Готово к использованию!

Новая структура упрощает работу с проектами и делает SEO модуль более организованным и удобным.

**Приятной работы!** 🚀
