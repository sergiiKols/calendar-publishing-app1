# 🔧 Настройка переменных окружения (.env файл)

## 📋 Пошаговая инструкция

### Шаг 1: Создать файл .env на сервере

```bash
# Скопировать пример
cp .env.production.example .env

# Или создать вручную
nano .env
```

### Шаг 2: Заполнить переменные

Скопируйте этот текст в ваш `.env` файл и замените значения:

```env
# ========================================
# DATABASE CONFIGURATION
# ========================================
POSTGRES_USER=calendar_user
POSTGRES_PASSWORD=CalendarPass2026Secure!
POSTGRES_DB=calendar_db
DATABASE_URL=postgresql://calendar_user:CalendarPass2026Secure!@db:5432/calendar_db

# ========================================
# NEXTAUTH CONFIGURATION
# ========================================
# ⚠️ ЗАМЕНИТЕ на ваш IP или домен!
NEXTAUTH_URL=http://your-server-ip:3000
NEXTAUTH_SECRET=abc123VeryLongSecretKey32charsHere!!

# ========================================
# ENCRYPTION & SECURITY
# ========================================
ENCRYPTION_KEY=def456AnotherSecretKey32chars!!

# ========================================
# ADMIN ACCOUNT
# ========================================
ADMIN_EMAIL=admin@example.com
ADMIN_PASSWORD=AdminPass123!

# ========================================
# SMI API TOKEN
# ========================================
SMI_API_TOKEN=ваш_smi_токен_здесь
CALENDAR_API_KEY=ваш_smi_токен_здесь

# ========================================
# CRON SECRET
# ========================================
CRON_SECRET=ваш_cron_секрет_здесь

# ========================================
# SYSTEM
# ========================================
NODE_ENV=production
NEXT_TELEMETRY_DISABLED=1
```

---

## ⚠️ ОБЯЗАТЕЛЬНО ИЗМЕНИТЕ:

### 1. **NEXTAUTH_URL** - URL вашего сервера

Примеры:
```env
# Если у вас IP адрес:
NEXTAUTH_URL=http://192.168.1.100:3000

# Если у вас домен:
NEXTAUTH_URL=https://calendar.example.com

# Локально для тестирования:
NEXTAUTH_URL=http://localhost:3000
```

### 2. **SMI_API_TOKEN** - токен от SMI проекта

Узнайте токен в вашем SMI проекте и вставьте:
```env
SMI_API_TOKEN=your_actual_token_here_123abc
CALENDAR_API_KEY=your_actual_token_here_123abc
```

### 3. **CRON_SECRET** - секрет для крона

Сгенерируйте случайную строку (можно любую):
```env
CRON_SECRET=my_super_secret_cron_key_2026
```

---

## 🔐 Опционально: Сгенерировать безопасные ключи

Если хотите создать более безопасные ключи:

```bash
# На сервере выполните:

# Для NEXTAUTH_SECRET
openssl rand -base64 32

# Для ENCRYPTION_KEY
openssl rand -base64 32

# Для CRON_SECRET
openssl rand -base64 24
```

Замените значения в `.env` на сгенерированные.

---

## ✅ Проверка правильности .env файла

После создания файла проверьте:

```bash
# Просмотреть файл
cat .env

# Убедитесь что:
# 1. Нет пробелов вокруг знака =
# 2. Все ключи заполнены (нет "ваш_токен_здесь")
# 3. NEXTAUTH_URL содержит ваш реальный IP или домен
```

---

## 🚀 Готово! Теперь можно запускать Docker

После настройки .env файла:

```bash
# Запустить проект
docker-compose up -d

# Проверить статус
docker-compose ps

# Посмотреть логи
docker-compose logs -f app
```

---

## 📝 Что означают переменные?

| Переменная | Описание |
|------------|----------|
| `POSTGRES_*` | Настройки базы данных PostgreSQL |
| `DATABASE_URL` | Строка подключения к БД (используется приложением) |
| `NEXTAUTH_URL` | URL вашего приложения (обязательно правильный!) |
| `NEXTAUTH_SECRET` | Секретный ключ для JWT токенов |
| `ENCRYPTION_KEY` | Ключ для шифрования паролей соц. сетей в БД |
| `ADMIN_EMAIL` | Email первого администратора |
| `ADMIN_PASSWORD` | Пароль первого администратора |
| `SMI_API_TOKEN` | Токен для получения статей от SMI |
| `CRON_SECRET` | Защита endpoint для автопубликации |

---

## ⚡ Быстрая настройка (минимум)

Если хотите быстро запустить, измените только эти 3 переменные:

```env
NEXTAUTH_URL=http://ВАШ_IP:3000          # ← Ваш IP адрес
SMI_API_TOKEN=ваш_реальный_токен         # ← Токен от SMI
CRON_SECRET=любая_секретная_строка       # ← Любой пароль
```

Остальное можно оставить как есть для первого запуска.
