# ⚡ Быстрый старт на сервере (5 минут)

## 1️⃣ Подготовка сервера (один раз)

```bash
# Установка Docker
sudo apt update && sudo apt install -y docker.io docker-compose git
sudo systemctl start docker && sudo systemctl enable docker
sudo usermod -aG docker $USER
# ПЕРЕЛОГИНИТЬСЯ после добавления в группу!
```

## 2️⃣ Скачать проект

```bash
cd ~
git clone https://github.com/sergiiKols/calendar-publishing-app1.git
cd calendar-publishing-app1
```

## 3️⃣ Настроить переменные

```bash
cp .env.docker.example .env
nano .env
```

**Обязательно изменить:**
```env
POSTGRES_PASSWORD=ваш_пароль_БД
NEXTAUTH_URL=http://ваш_IP:3000
NEXTAUTH_SECRET=$(openssl rand -base64 32)
ENCRYPTION_KEY=$(openssl rand -base64 32)
ADMIN_EMAIL=admin@example.com
ADMIN_PASSWORD=ваш_пароль_админа
```

## 4️⃣ Запустить всё (база, приложение, cron)

```bash
docker-compose up -d --build
```

## 5️⃣ Проверить

```bash
# Статус контейнеров
docker-compose ps

# Логи
docker-compose logs -f

# Открыть в браузере
# http://ВАШ_IP:3000/login
```

---

## 🔥 Основные команды

```bash
# Остановить
docker-compose down

# Запустить
docker-compose up -d

# Перезапустить
docker-compose restart

# Обновить код с GitHub
git pull origin main
docker-compose up -d --build

# Бэкап БД
docker-compose exec db pg_dump -U calendar_user calendar_db > backup.sql

# Восстановить БД
docker-compose exec -T db psql -U calendar_user calendar_db < backup.sql

# Логи
docker-compose logs -f app      # приложение
docker-compose logs -f db       # база данных
docker-compose logs -f cron     # планировщик
```

---

## 📋 Что включено?

✅ **PostgreSQL** - база данных  
✅ **Next.js приложение** - веб-интерфейс (порт 3000)  
✅ **Cron планировщик** - автопубликация каждые 5 минут  
✅ **Автоинициализация БД** - таблицы создаются автоматически  
✅ **Автосоздание админа** - из переменных ADMIN_EMAIL/PASSWORD  

---

## 🆘 Проблемы?

```bash
# Посмотреть что пошло не так
docker-compose logs --tail=100

# Пересоздать всё с нуля
docker-compose down -v
docker-compose up -d --build
```

Подробная инструкция: [ПОШАГОВЫЙ_ДЕПЛОЙ.md](./ПОШАГОВЫЙ_ДЕПЛОЙ.md)
