# 🐳 Docker развертывание Calendar Publishing App

## 📦 Что создано

Полная Docker конфигурация для развертывания на вашем сервере:

### Файлы конфигурации:
- ✅ `Dockerfile` - образ Next.js приложения
- ✅ `docker-compose.yml` - оркестрация сервисов
- ✅ `.env.docker.example` - шаблон переменных окружения
- ✅ `.dockerignore` - исключения для Docker build

### Скрипты:
- ✅ `deploy.sh` - автоматическое развертывание
- ✅ `docker-helpers.sh` - вспомогательные команды

### Документация:
- ✅ `QUICK_START_DOCKER.md` - быстрый старт (5 минут)
- ✅ `DOCKER_DEPLOYMENT.md` - полная документация
- ✅ `nginx.conf.example` - конфигурация Nginx (опционально)

---

## 🚀 Быстрый старт (3 шага)

### 1️⃣ Скопировать проект на сервер
```bash
# На вашем компьютере
scp -r calendar-app user@your-server:/home/user/

# Или через git
git clone your-repo
cd calendar-app
```

### 2️⃣ Настроить переменные окружения
```bash
cd calendar-app
cp .env.docker.example .env

# Отредактировать .env (установить пароли и секреты)
nano .env
```

### 3️⃣ Запустить
```bash
# Если есть bash на сервере
bash deploy.sh

# Или вручную
docker-compose up -d
```

**Готово!** Приложение доступно на `http://your-server:3000`

---

## 📊 Что будет развернуто

| Сервис | Порт | Назначение |
|--------|------|------------|
| **calendar-app** | 3000 | Next.js приложение |
| **postgres** | 5432 | PostgreSQL база данных |

### Ресурсы:
- **RAM**: ~500-800 MB
- **Диск**: ~1.5 GB
- **CPU**: минимум 2 ядра

---

## 🔧 Управление

```bash
# Запустить
docker-compose up -d

# Остановить
docker-compose down

# Посмотреть логи
docker-compose logs -f

# Перезапустить
docker-compose restart

# Обновить приложение
docker-compose down
docker-compose build --no-cache
docker-compose up -d
```

---

## 📁 Структура данных

### База данных PostgreSQL:
- **Контейнер**: `calendar-postgres`
- **Том данных**: `postgres_data` (персистентное хранилище)
- **Автоматическая инициализация**: схема создается при первом запуске

### Backup базы данных:
```bash
docker-compose exec postgres pg_dump -U calendar_user calendar_db > backup.sql
```

### Restore базы данных:
```bash
cat backup.sql | docker-compose exec -T postgres psql -U calendar_user calendar_db
```

---

## 🌐 Настройка домена и HTTPS

Смотрите `nginx.conf.example` для настройки reverse proxy с SSL.

---

## 🆘 Troubleshooting

### Проблема: Порт 3000 занят
```bash
# Изменить порт в docker-compose.yml
ports:
  - "8080:3000"  # Вместо 3000:3000
```

### Проблема: База данных не инициализируется
```bash
# Пересоздать тома
docker-compose down -v
docker-compose up -d
```

### Проблема: Недостаточно памяти
```bash
# Проверить доступную память
free -h

# Добавить swap если нужно
sudo fallocate -l 2G /swapfile
sudo chmod 600 /swapfile
sudo mkswap /swapfile
sudo swapon /swapfile
```

---

## 📚 Документация

- **Быстрый старт**: `QUICK_START_DOCKER.md` - для начинающих
- **Полная документация**: `DOCKER_DEPLOYMENT.md` - детальное руководство
- **Основной README**: `README.md` - описание проекта

---

## ✅ Следующие шаги

1. ✅ Развернуть на сервере
2. ⚙️ Настроить Nginx + SSL (опционально)
3. 🔐 Создать админа: `http://your-server:3000/api/reset-admin`
4. 📱 Подключить социальные сети в интерфейсе
5. 📊 Настроить мониторинг (опционально)

---

## 💡 Полезные команды

```bash
# Проверить статус контейнеров
docker-compose ps

# Зайти в контейнер приложения
docker-compose exec app sh

# Зайти в PostgreSQL
docker-compose exec postgres psql -U calendar_user calendar_db

# Посмотреть использование ресурсов
docker stats

# Очистить неиспользуемые образы
docker system prune -a
```

---

## 🔒 Безопасность

⚠️ **Обязательно измените в `.env`:**
- `POSTGRES_PASSWORD` - пароль базы данных
- `NEXTAUTH_SECRET` - секрет аутентификации
- `CALENDAR_API_KEY` - API ключ
- `CRON_SECRET` - секрет для cron jobs

---

**Готово к развертыванию! 🚀**

Для помощи смотрите `QUICK_START_DOCKER.md`
