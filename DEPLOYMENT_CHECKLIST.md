# ✅ Чеклист развертывания Calendar App в Docker

## 📋 Пошаговый чеклист

### Этап 1: Подготовка сервера

- [ ] Сервер с Linux (Ubuntu/Debian рекомендуется)
- [ ] Минимум 2GB RAM
- [ ] Минимум 2GB свободного места на диске
- [ ] SSH доступ к серверу
- [ ] Docker установлен (`docker --version`)
- [ ] Docker Compose установлен (`docker-compose --version`)
- [ ] Пользователь добавлен в группу docker (`sudo usermod -aG docker $USER`)
- [ ] Порты 3000 и 5433 свободны

```bash
# Проверка требований
docker --version        # Должно показать версию
docker-compose --version
free -h                 # Проверить RAM
df -h                   # Проверить диск
ss -tulpn | grep :3000  # Порт должен быть свободен
```

---

### Этап 2: Перенос проекта на сервер

Выберите один из способов:

#### Вариант A: Git
- [ ] Репозиторий доступен на сервере
- [ ] Выполнено: `git clone <url>`
- [ ] Выполнено: `cd calendar-app`

#### Вариант B: SCP
- [ ] Выполнено: `scp -r calendar-app user@server:/path/`
- [ ] Выполнено: `ssh user@server`
- [ ] Выполнено: `cd /path/calendar-app`

#### Вариант C: Архив
- [ ] Создан архив: `tar -czf calendar-app.tar.gz calendar-app/`
- [ ] Передан на сервер: `scp calendar-app.tar.gz user@server:`
- [ ] Распакован: `tar -xzf calendar-app.tar.gz`
- [ ] Выполнено: `cd calendar-app`

---

### Этап 3: Настройка переменных окружения

- [ ] Выполнено: `cp .env.docker.example .env`
- [ ] Файл .env открыт для редактирования: `nano .env`

#### Обязательные изменения в .env:

- [ ] `POSTGRES_PASSWORD` - изменен на сложный пароль
- [ ] `NEXTAUTH_SECRET` - сгенерирован: `openssl rand -base64 32`
- [ ] `CALENDAR_API_KEY` - установлен уникальный ключ
- [ ] `CRON_SECRET` - установлен уникальный секрет

#### Опциональные настройки:

- [ ] `NEXTAUTH_URL` - изменен на ваш домен (если есть)
- [ ] `POSTGRES_USER` - изменен (опционально)
- [ ] `POSTGRES_DB` - изменен (опционально)

```bash
# Генерация секретов
openssl rand -base64 32  # Для NEXTAUTH_SECRET
openssl rand -hex 16     # Для CALENDAR_API_KEY
openssl rand -hex 16     # Для CRON_SECRET
```

---

### Этап 4: Запуск Docker

- [ ] Выполнено: `docker-compose up -d`
- [ ] Контейнеры запущены: `docker-compose ps`
- [ ] Все 3 контейнера в статусе "Up":
  - [ ] calendar-postgres
  - [ ] calendar-nextjs
  - [ ] calendar-cron

```bash
# Проверка
docker-compose ps

# Ожидаемый результат:
# NAME               STATUS          PORTS
# calendar-postgres  Up 1 minute     0.0.0.0:5433->5432/tcp
# calendar-nextjs    Up 1 minute     0.0.0.0:3000->3000/tcp
# calendar-cron      Up 1 minute
```

---

### Этап 5: Проверка логов

- [ ] Логи PostgreSQL без ошибок: `docker-compose logs postgres`
- [ ] База данных инициализирована (видны таблицы)
- [ ] Логи приложения без критических ошибок: `docker-compose logs app`
- [ ] Next.js успешно запущен (видно: "Ready in XXms")
- [ ] Cron запущен: `docker-compose logs cron`

```bash
# Проверка логов
docker-compose logs -f

# Остановить просмотр: Ctrl+C

# Проверить последние 50 строк
docker-compose logs --tail=50 app
```

---

### Этап 6: Создание администратора

- [ ] Открыт в браузере: `http://your-server-ip:3000/api/reset-admin`
- [ ] Получен ответ с credentials
- [ ] Записаны учетные данные:
  - Username: `admin`
  - Password: `admin123`

```bash
# Или через curl на сервере
curl http://localhost:3000/api/reset-admin

# Ожидаемый результат:
# {"success":true,"credentials":{"username":"admin","password":"admin123"}}
```

---

### Этап 7: Первый вход в систему

- [ ] Открыто: `http://your-server-ip:3000/login`
- [ ] Успешный вход с admin/admin123
- [ ] Открыт календарь: `http://your-server-ip:3000/calendar`
- [ ] Интерфейс загружается корректно

---

### Этап 8: Настройка безопасности

- [ ] Пароль админа изменен в интерфейсе
- [ ] Firewall настроен:
  ```bash
  sudo ufw allow 22/tcp   # SSH
  sudo ufw allow 3000/tcp # App
  sudo ufw enable
  ```
- [ ] SSH ключи настроены (вместо паролей)
- [ ] Backup .env файла создан: `cp .env .env.backup`

---

### Этап 9: Настройка публикации (опционально)

- [ ] Добавлен хотя бы один publishing platform в интерфейсе
- [ ] Протестирована отправка статьи из SMI проекта
- [ ] Создано тестовое событие в календаре
- [ ] Проверена автопубликация (подождать 15 минут)

```bash
# Проверка cron логов
docker-compose logs -f cron

# Проверка публикаций
docker-compose exec postgres psql -U calendar_user calendar_db \
  -c "SELECT * FROM publish_logs ORDER BY created_at DESC LIMIT 5;"
```

---

### Этап 10: Настройка домена и HTTPS (опционально)

Если есть домен:

- [ ] A-запись домена указывает на IP сервера
- [ ] DNS записи обновлены (может занять до 24 часов)
- [ ] Nginx установлен: `sudo apt install nginx`
- [ ] Скопирована конфигурация: `sudo cp nginx.conf.example /etc/nginx/sites-available/calendar`
- [ ] В конфиге изменен `your-domain.com` на реальный домен
- [ ] Конфиг активирован: `sudo ln -s /etc/nginx/sites-available/calendar /etc/nginx/sites-enabled/`
- [ ] Nginx перезапущен: `sudo systemctl reload nginx`
- [ ] Certbot установлен: `sudo apt install certbot python3-certbot-nginx`
- [ ] SSL сертификат получен: `sudo certbot --nginx -d your-domain.com`
- [ ] HTTPS работает: `https://your-domain.com`
- [ ] В .env изменен `NEXTAUTH_URL=https://your-domain.com`
- [ ] Приложение перезапущено: `docker-compose restart app`

---

### Этап 11: Настройка backup (рекомендуется)

- [ ] Создана папка для backup: `mkdir -p ~/backups`
- [ ] Протестирован backup:
  ```bash
  docker-compose exec postgres pg_dump -U calendar_user calendar_db \
    > ~/backups/calendar_test.sql
  ```
- [ ] Проверен размер backup: `ls -lh ~/backups/`
- [ ] Добавлен cron job для автоматического backup:
  ```bash
  crontab -e
  # Добавить:
  0 3 * * * cd /path/to/calendar-app && docker-compose exec postgres pg_dump -U calendar_user calendar_db > ~/backups/calendar_$(date +\%Y\%m\%d).sql
  ```
- [ ] Настроена очистка старых backup (старше 30 дней):
  ```bash
  0 4 * * * find ~/backups -name "calendar_*.sql" -mtime +30 -delete
  ```

---

### Этап 12: Мониторинг (опционально)

- [ ] Проверено использование ресурсов: `docker stats`
- [ ] Настроен мониторинг логов:
  ```bash
  # Создать скрипт для проверки
  nano ~/check-calendar.sh
  ```
  ```bash
  #!/bin/bash
  if ! docker-compose -f /path/to/calendar-app/docker-compose.yml ps | grep -q "Up"; then
    echo "Calendar app is down!" | mail -s "Alert" your@email.com
  fi
  ```
- [ ] Добавлен в cron:
  ```bash
  */5 * * * * ~/check-calendar.sh
  ```

---

## ✅ Финальная проверка

### Всё работает если:

- [x] Все 3 контейнера в статусе "Up"
- [x] Приложение доступно по `http://your-server-ip:3000`
- [x] Можно войти в систему
- [x] Календарь отображается корректно
- [x] База данных содержит таблицы
- [x] Логи без критических ошибок
- [x] Cron job работает (проверить через 15 минут)

### Тестовые команды:

```bash
# 1. Статус контейнеров
docker-compose ps

# 2. Использование ресурсов
docker stats --no-stream

# 3. Проверка БД
docker-compose exec postgres psql -U calendar_user calendar_db -c "\dt"

# 4. Проверка приложения
curl http://localhost:3000/api/health || echo "Создайте health endpoint"

# 5. Размер данных
docker system df

# 6. Логи последние 100 строк
docker-compose logs --tail=100
```

---

## 🆘 Troubleshooting

### ❌ Контейнеры не запускаются

```bash
# Проверить логи
docker-compose logs

# Пересоздать
docker-compose down -v
docker-compose up -d
```

### ❌ База данных не инициализируется

```bash
# Вручную создать схему
docker-compose exec postgres psql -U calendar_user calendar_db < lib/db/schema.sql
```

### ❌ Приложение не подключается к БД

```bash
# Проверить сеть
docker network ls
docker network inspect calendar_calendar-network

# Проверить .env
cat .env | grep POSTGRES
```

### ❌ Порт занят

```bash
# Найти процесс
sudo lsof -i :3000

# Или изменить порт в docker-compose.yml
ports:
  - "8080:3000"
```

---

## 📞 Полезные команды

```bash
# Перезапуск
docker-compose restart

# Остановка
docker-compose down

# Логи в реальном времени
docker-compose logs -f

# Зайти в контейнер
docker-compose exec app sh
docker-compose exec postgres psql -U calendar_user calendar_db

# Обновление
git pull
docker-compose down
docker-compose build --no-cache
docker-compose up -d

# Очистка
docker system prune -a
```

---

## 🎉 Поздравляем!

Если все пункты отмечены ✅, ваш Calendar App успешно развернут и работает!

**Следующие шаги:**
1. Настроить интеграцию с SMI проектом
2. Добавить платформы для публикации
3. Создать первое событие в календаре
4. Протестировать автопубликацию

**Документация:**
- Управление: `DOCKER_README.md`
- Команды: `DEPLOY_CHEATSHEET.md`
- Архитектура: `ARCHITECTURE_DOCKER.md`
