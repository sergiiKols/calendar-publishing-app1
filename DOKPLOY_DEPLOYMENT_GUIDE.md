# 🚀 Руководство по развертыванию через Dokploy

## Для развертывания Calendar App на домене https://calendar.energo-audit.online

---

## 📋 Шаг 1: Настройка в Dokploy

### 1.1 Создание проекта PostgreSQL Database

1. В Dokploy перейдите в раздел **Databases**
2. Нажмите **Create Database** → **PostgreSQL**
3. Укажите параметры:
   - **Name**: `calendar-db` (или как у вас названо)
   - **Database**: `calendar_db`
   - **Username**: `calendar_user`
   - **Password**: `CalendarPass2026Secure`
4. Запомните **имя контейнера** (например: `calendarpublishingapp1-calendardb-0rolji`)

### 1.2 Создание приложения Next.js

1. В Dokploy перейдите в раздел **Applications**
2. Нажмите **Create Application**
3. Выберите тип: **Docker** или **Git**
4. Укажите:
   - **Name**: `calendar-app`
   - **Build Path**: `calendar-app` (если репозиторий содержит несколько проектов)

---

## 📝 Шаг 2: Настройка переменных окружения

В разделе **Environment** вашего приложения добавьте следующие переменные:

### База данных
```
POSTGRES_USER=calendar_user
POSTGRES_PASSWORD=CalendarPass2026Secure
POSTGRES_DB=calendar_db
```

### Database URLs (ВАЖНО! Используйте имя контейнера БД из Dokploy)
```
DATABASE_URL=postgresql://calendar_user:CalendarPass2026Secure@calendarpublishingapp1-calendardb-0rolji:5432/calendar_db
POSTGRES_URL=postgresql://calendar_user:CalendarPass2026Secure@calendarpublishingapp1-calendardb-0rolji:5432/calendar_db
POSTGRES_PRISMA_URL=postgresql://calendar_user:CalendarPass2026Secure@calendarpublishingapp1-calendardb-0rolji:5432/calendar_db?pgbouncer=true&connect_timeout=15
POSTGRES_URL_NON_POOLING=postgresql://calendar_user:CalendarPass2026Secure@calendarpublishingapp1-calendardb-0rolji:5432/calendar_db
```

**⚠️ ЗАМЕНИТЕ `calendarpublishingapp1-calendardb-0rolji` на фактическое имя вашего контейнера БД!**

### NextAuth
```
NEXTAUTH_URL=https://calendar.energo-audit.online
NEXTAUTH_SECRET=aB3xK9mP2qR5sT7uV1wX4yZ6cD8eF0gH1iJ3kL5mN7oP9qR
```

### API Keys
```
CALENDAR_API_KEY=1267778366
SMI_API_TOKEN=1267778366
CRON_SECRET=1267778366
```

### Безопасность
```
ENCRYPTION_KEY=Ql&1.Z}CWpv>yX+3PfC=$\p*tp8be6~&
```

### Admin аккаунт
```
ADMIN_EMAIL=admin@example.com
ADMIN_PASSWORD=AdminPass123!
```

### Прочее
```
NODE_ENV=production
NEXT_TELEMETRY_DISABLED=1
```

---

## 🌐 Шаг 3: Настройка домена

### 3.1 В разделе **Domains**

1. Перейдите во вкладку **Domains**
2. Нажмите **Add Domain**
3. Укажите:
   - **Domain**: `calendar.energo-audit.online`
   - **Port**: `3000`
   - **Protocol**: `HTTPS`
   - **SSL Certificate**: `Let's Encrypt` (автоматически)
4. Сохраните

### 3.2 Убедитесь, что:
- ✅ DNS запись для `calendar.energo-audit.online` указывает на IP вашего сервера
- ✅ Порт 443 (HTTPS) открыт на сервере
- ✅ SSL сертификат успешно выпущен (зеленый статус в Dokploy)

---

## 🏗️ Шаг 4: Настройка Docker build

### 4.1 Убедитесь, что в корне проекта есть:

- `Dockerfile` ✅
- `docker-compose.yml` ✅ (может не использоваться Dokploy)
- `.dockerignore` ✅

### 4.2 Dockerfile уже настроен правильно
Multi-stage build с standalone выводом Next.js

---

## 🚀 Шаг 5: Развертывание

### 5.1 Запуск сборки

1. В Dokploy перейдите в раздел **Deployments**
2. Нажмите **Deploy**
3. Дождитесь завершения сборки (5-10 минут)

### 5.2 Проверка логов

В разделе **Logs** проверьте:
- ✅ Сборка Next.js прошла успешно
- ✅ Приложение запустилось на порту 3000
- ✅ Подключение к базе данных успешно

Ищите строки:
```
✓ Ready in XXXms
✓ Compiled successfully
Server listening on 0.0.0.0:3000
```

---

## 🔍 Шаг 6: Инициализация базы данных

### 6.1 Первый запуск

При первом запуске приложения **автоматически**:
1. Создаются таблицы в БД (из `lib/db/schema.sql`)
2. Создается администратор с данными из `ADMIN_EMAIL` и `ADMIN_PASSWORD`

### 6.2 Если БД не инициализировалась

Вы можете вручную выполнить SQL из файла `lib/db/schema.sql` через:
- Dokploy → Database → **Query Console**
- Или любой PostgreSQL клиент (DBeaver, pgAdmin)

---

## ✅ Шаг 7: Проверка работы

1. Откройте https://calendar.energo-audit.online
2. Должна открыться страница входа
3. Войдите с данными:
   - Email: `admin@example.com`
   - Password: `AdminPass123!`
4. После входа должен открыться календарь

---

## 🐛 Типичные проблемы и решения

### Проблема 1: "Unable to connect to database"

**Причина**: Неправильное имя контейнера БД в DATABASE_URL

**Решение**:
1. В Dokploy → Database → найдите имя контейнера (например: `calendarpublishingapp1-calendardb-0rolji`)
2. Обновите все переменные `DATABASE_URL`, `POSTGRES_URL` и т.д.
3. Пересоберите приложение

### Проблема 2: "NEXTAUTH_URL is not defined"

**Причина**: Переменная окружения не установлена

**Решение**:
1. В Dokploy → Environment добавьте: `NEXTAUTH_URL=https://calendar.energo-audit.online`
2. Пересоберите

### Проблема 3: Сайт не открывается / SSL ошибка

**Причина**: DNS не настроен или SSL сертификат не выпущен

**Решение**:
1. Проверьте DNS: `nslookup calendar.energo-audit.online` должен вернуть IP вашего сервера
2. В Dokploy → Domains → проверьте статус SSL (должен быть зеленый)
3. Подождите 5-10 минут для выпуска сертификата

### Проблема 4: "Error: P1001: Can't reach database server"

**Причина**: БД не запущена или недоступна

**Решение**:
1. В Dokploy → Databases → убедитесь что статус БД "Running"
2. Проверьте имя контейнера БД в переменных окружения
3. Убедитесь что приложение и БД в одной Docker сети

### Проблема 5: Приложение постоянно перезапускается

**Причина**: Ошибка при запуске (обычно связана с БД)

**Решение**:
1. Посмотрите логи в Dokploy → Logs
2. Найдите строки с "Error" или "Failed"
3. Чаще всего это проблема с DATABASE_URL

---

## 📊 Мониторинг

### Полезные команды в логах Dokploy:

Ищите эти строки для диагностики:

**Успешный запуск:**
```
✓ Ready in 1234ms
Server listening on 0.0.0.0:3000
```

**Проблемы с БД:**
```
Error: P1001: Can't reach database server
Unable to connect to the database
```

**Проблемы с NextAuth:**
```
NEXTAUTH_URL is not defined
NEXTAUTH_SECRET must be provided
```

---

## 🔄 Обновление приложения

1. В Dokploy → Deployments → **Redeploy**
2. Или через Git: push изменения → автодеплой (если настроен webhook)

---

## 📞 Контакты и поддержка

Если возникли проблемы:
1. Проверьте логи в Dokploy
2. Убедитесь что все переменные окружения установлены
3. Проверьте статус БД и приложения

---

**Дата создания:** 2026-02-24  
**Версия:** 1.0
**Домен:** https://calendar.energo-audit.online
